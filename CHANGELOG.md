# 변경 내역

새로 바뀐 것이 위에 옵니다.

판 번호(v1.0 같은 것)는 게임의 뼈대가 크게 달라질 때만 올립니다.
버그 수정이나 화면 손질은 번호를 올리지 않고 날짜별로 여기에만 적습니다.

---

## v1.0 이후 — 2026-08-14

판 번호는 그대로 v1.0 입니다. 버그 수정입니다.

### 버그 수정

**포메이션을 바꾸면 대전 코드가 발급되지 않던 문제**
5-3-2 로 짜 두었다가 4-3-3 으로 바꾸는 식으로 포메이션을 옮기면, 화면에는 "선발 11명"
이라고 나오는데 코드 발급을 누르면 "○○ 자리가 비어 있습니다" 라며 막히는 경우가
있었습니다.

전술판은 스물다섯 자리를 모두 보여 주는데, 실제로 경기에 나가는 것은 그중 지금
포메이션이 쓰는 열한 자리뿐입니다. 그런데 화면이 그 구분을 하지 않아, 지금 포메이션에
없는 자리에도 선수를 세울 수 있었습니다. 그러면 사람 수는 열한 명이 맞지만 정작
필요한 자리가 비어 있어 코드를 만들 수 없었습니다.

**골 해설이 2D 장면보다 먼저 나오던 문제**
골이 들어가는 장면을 보기도 전에 해설로 "골!" 을 먼저 읽게 돼, 정작 볼거리인 2D 가
김이 새 버렸습니다. 전광판 점수도 장면보다 먼저 올라갔습니다.

이제 **골 해설은 공이 골망에 꽂히는 그 순간에 나옵니다.** 장면이 재생되는 동안에는
자막과 화면 위 이름표도 결과를 감춥니다 — 몇 분인지만 보이고 "골" 인지 아닌지는
들어가는 순간에 드러납니다.

건너뛰거나 끝까지 감기를 눌러도 붙들어 둔 해설은 빠짐없이 나옵니다.

### 새로 생긴 것

**전술 게시판 — 상대를 못 구해도 붙을 수 있습니다**

코드를 주고받는 것 자체는 잘 되는데, **상대를 구하는 일**이 번거로웠습니다.
게시판에 글을 따로 쓰고, 댓글을 기다리고, 링크를 눌러 페이지를 옮겨야 했으니까요.

이제 라인업 화면에서 `🔗 대전 코드 발급` 을 누르면 그 자리에 **`📋 전술 게시판에 올리기`**
가 함께 나옵니다. **닉네임만 적고 올리면 끝입니다.** 다른 사람은 경기 화면 원정 칸의
**`📋 전술 게시판에서 고르기`** 에서 목록을 열어 그 라인업을 바로 상대로 세웁니다.

- **상대가 접속해 있을 필요가 없습니다.** 남겨 둔 라인업과 붙는 것입니다.
- **페이지를 옮기지 않습니다.** 코드를 복사해 어디에 붙일 일도 없습니다.
- 올라가는 것은 **닉네임 · 한 줄 소개 · 구단 · 대전 코드** 뿐입니다.
  로그인도 개인정보도 없습니다.

> 게시판이 안 보인다면 브라우저가 옛 파일을 쓰고 있는 것입니다 —
> 강력 새로고침(`Ctrl+Shift+R`)을 해 보세요.

### 화면

**조건부 지시가 발동했는지 알 수 있게 했습니다**

경기 중에는 아무도 버튼을 못 누르므로 **조건부 지시가 유일한 개입 수단**인데, 발동해도
회색 글씨로 다른 해설에 섞여 흘러가 버려서 실행됐는지조차 알기 어려웠습니다.

- 발동한 순간 해설창에 **금색 카드**로 세워 눈에 걸리게 했습니다.
  해설이 아니라 "내가 미리 적어 둔 것이 실행됐다"는 표시입니다.
- 경기가 끝나면 **경기 기록 아래에 요약**이 붙습니다 — 몇 분에 어떤 지시가 발동했는지
  시간순으로 모아 보여 줍니다. 하나도 발동하지 않았으면 그렇다고 알려 줍니다.

> 조건이 맞았더라도 전술이 **이미 그 상태였다면** 바꿀 것이 없어 줄이 남지 않습니다.
> 예를 들어 압박이 이미 최대인데 "압박을 올린다"가 걸리면 아무 일도 일어나지 않습니다.

**2D 바로 아래에 "지금 막 벌어진 일" 한 줄**

눈은 필드에 가 있는데 해설은 아래쪽 기록에 쌓이니, 무슨 일이 벌어졌는지 보려면
시선을 아래까지 내려야 했습니다.

이제 **2D 화면 바로 아래에 한 줄**이 붙어 가장 새 소식만 보여 줍니다. 바뀔 때 한 번
번쩍이므로 놓치지 않습니다. 아래 기록은 지금까지처럼 그대로 쌓입니다 — 두 개가 함께 있습니다.

**경기 시작 직후가 죽은 화면처럼 보이던 것**

경기 초반에는 결정적 장면이 없어서 2D 가 아주 빠르게 흘러갔고, 해설도 몇 줄 없는데
상자 **아래쪽**에 붙어 있어서 화면 위쪽이 통째로 비었습니다. 그래서 시작하자마자
"멈춰 있는 검은 화면"처럼 보였습니다. 두 가지를 함께 고쳤습니다.

- **처음 3분은 빨리 감지 않습니다.** 결정적 장면을 보여 줄 때와 같은 속도로, **같은 밝기**로
  흐릅니다(`▶ 경기 시작`). 경기가 어떻게 시작하는지를 보고 나서 나머지가 빨라지고,
  그때부터 화면이 어두워지며 `○ 경기 진행 중` 으로 바뀝니다 — 어두운 것은
  "지금은 빨리 감는 중"이라는 표시입니다.
- **해설이 위에서부터 찹니다.** 새 줄이 맨 위에 붙고 지나간 줄이 아래로 밀려 내려갑니다.
  예전에는 아래에서 위로 쌓여서 초반에 위쪽이 비어 보였습니다.

전체 재생 시간은 90분 기준 1분 12초에서 **1분 30초쯤**으로 늘어납니다.
급하시면 2·4배속이나 `⏭ 끝까지`를 쓰시면 됩니다.

**선발 명단 정렬**
자리 이름표(ST · LAM 처럼 글자 수가 다른 것)의 폭을 고정해, 선수 이름과 아래 역할 칸이
줄마다 같은 자리에서 시작하도록 맞췄습니다.

**전술판에서 쓰지 않는 자리가 구분됩니다**

지금 포메이션이 쓰지 않는 자리는 **흐리게** 보이고 선수를 세울 수 없습니다.
누르거나 끌어다 놓으면 **어느 포메이션에서 그 자리가 열리는지** 알려 줍니다
(예: "4-3-3 에는 LM 자리가 없습니다 — 3-4-2-1 · 3-4-3 · 4-1-4-1 에서 열립니다").
그 칸에 손을 얹어도 같은 안내가 보입니다.

어느 포메이션도 쓰지 않는 `SW` 칸은 전술판에서 아예 없앴습니다.

저장해 둔 라인업을 불러올 때, 그 자리들을 모두 담는 포메이션이 있으면 그쪽으로 맞춰 줍니다.

**골키퍼의 선호 포지션이 엉뚱하게 나오던 문제**
골키퍼인데 선호 포지션이 중앙 미드필더나 공격수로 표시되는 선수가 **열 명** 있었습니다
(안양 김정훈, 울산 류성민, 전북 이한결·정대영, 포항 홍성민, 서울 윤기욱, 제주 허재원,
인천 왕민준, 광주 김동화, 부천 김찬영).

선호 포지션 표를 **이름으로만** 찾는데, 실제 등록 명단에 같은 이름의 다른 선수가 있으면
그 사람의 자리가 골키퍼에게 넘어오고 있었습니다. 이제 골키퍼는 골키퍼만, 필드 플레이어는
골키퍼가 아닌 자리만 선호로 갖습니다.

반대 경우도 하나 있어 함께 고쳤습니다 — 포항 김호진 선수는 수비수인데 골키퍼를 선호로
들고 있어서 골문 앞에 세워도 아무 문제가 없는 것처럼 보였습니다.

### 알아 두실 점

선수 데이터가 바뀌었지만 **명단·능력치는 하나도 달라지지 않았습니다.** 그래서 지금까지
나눠 가진 **대전 코드는 그대로 쓸 수 있습니다.** 예전 코드를 넣으면 화면이 "옛 판 코드"
라고 알려 주고 그대로 읽습니다.

**결과 링크**는 포항이 낀 경기만 결과가 달라질 수 있습니다(김호진 선수의 소화 가능 자리가
바뀌었기 때문입니다). 나머지 구단은 영향이 없습니다.

---

## v1.0 이후 — 2026-08-13 (두 번째)

판 번호는 그대로 v1.0 입니다. 버그 수정입니다.

### 버그 수정

**결과 링크를 열면 전혀 다른 경기가 재생되던 문제**
같은 링크인데 열 때마다 점수도 득점자도 달랐습니다. 경기의 무작위 씨앗을 대전 코드가
아니라 화면이 들고 있던 라인업에서 뽑고 있었던 탓입니다. 화면에서 짠 라인업과 코드에서
되살린 라인업은 속이 조금 달라서, 같은 라인업인데도 씨앗이 갈렸습니다.
이제 두 사람이 실제로 주고받는 대전 코드에서 씨앗을 뽑습니다 — 코드가 같으면 언제
어디서 열어도 같은 경기가 나옵니다.
경기 중 **끝까지 버튼은 이 문제와 무관**했습니다. 경기는 미리 다 계산해 두고 화면은
재생만 하므로, 얼마나 보다가 눌러도 결과가 바뀌지 않습니다. 버튼은 그대로 둡니다.

**교체 명단 번호(S1~S9)가 아무 의미가 없던 문제**
화면에는 번호를 붙여 놓고 실제로는 능력치 순으로 아무나 들어갔습니다.
이제 번호가 곧 투입 우선순위입니다. 먼저 쓰고 싶은 선수를 위로 올려 두세요.

**지고 있을 때의 교체가 팀을 더 약하게 만들던 문제**
지고 있으면 수비수를 빼고 공격수를 넣었는데, 교체 선수는 나간 선수의 자리를 그대로
물려받기 때문에 스트라이커가 센터백 자리에 서 있었습니다. 수비는 무너지고 공격은
늘지 않았습니다. 이제 그 자리를 실제로 소화할 수 있는 선수 중에서 고릅니다.

**부상으로 빈 자리가 끝까지 안 채워지던 문제**
선수가 다쳐 나가도 아무도 들어오지 않아 남은 시간을 열 명으로 뛰었습니다.
이제 부상으로 자리가 비면 시간과 상관없이 곧바로 채웁니다.

### 화면

**교체 명단에 자동 교체 규칙 안내 추가**
언제(후반 여섯 번), 누구를(카드·체력·점수 상황), 어떤 순서로(번호 순) 바꾸는지를
라인업 화면 교체 명단 위에 적었습니다. 골키퍼는 자동 교체하지 않습니다.

### 알아 두실 점

씨앗을 뽑는 방식이 바뀌었으므로, **이전에 만든 결과 링크는 그때와 다른 경기를 보여
줍니다.** 지금까지는 어떤 경로로 들어오느냐에 따라 결과가 갈렸으니 애초에 고정된 값이
없었습니다. 지금 만드는 링크부터는 항상 같은 경기가 재생됩니다.

---

## v1.0 이후 — 2026-08-13

판 번호는 그대로 v1.0 입니다. 버그 수정과 화면 손질입니다.

### 버그 수정

**왼쪽·오른쪽 풀백이 전부 중앙 수비수로 나오던 문제**
측면 수비수로 뛰어야 할 선수들이 화면에 죄다 중앙 수비수로 표시되고 있었습니다.
선수 132명의 선호 자리를 실제 뛰는 자리로 바로잡았습니다.

**업데이트 뒤에 예전 대전 코드가 안 읽히던 문제**
위 수정으로 선수 데이터가 바뀌면서, 그 전에 발급해 주고받은 대전 코드가 전부
막혔습니다. 확인해 보니 선수 명단 자체는 그대로였기에, 예전 코드도 계속 쓸 수
있도록 열었습니다. 예전 코드로 들어오면 화면에 그 사실을 알려 줍니다.

**연습 경기 뒤에 결과 공유 링크 칸이 사라지던 문제**
연습 경기는 상대를 컴퓨터가 그 자리에서 짜기 때문에 원래 링크를 만들 수 없는데,
아무 설명 없이 칸만 사라져서 기능이 없어진 것처럼 보였습니다.
이제 왜 링크가 없는지와 사람과 대전하는 방법을 그 자리에 알려 줍니다.

### 화면

**구단 전력을 별점으로 표시**
라인업 화면과 선수단 화면에서, 구단을 눌러 보기 전에도 그 팀이 얼마나 강한지
별 다섯 개로 바로 확인할 수 있습니다.
K리그1 열두 팀이 모두 비슷한 별로 몰리지 않도록 팀끼리 비교하기 좋은 눈금을 씁니다.

**전술 화면에서 내부 수치 감추기**
전술 조절 막대 설명에 개발하면서 적어 둔 테스트용 숫자가 그대로 보이고 있어서
화면에서 없앴습니다. 무엇을 얻고 무엇을 잃는지에 대한 설명은 그대로 있습니다.

### 그 밖에

- 선수 데이터를 만드는 도구가 선호 자리 표를 빠뜨리던 것을 고쳐, 다시 만들어도
  같은 문제가 나지 않게 했습니다.
- 접속하는 기기가 예전 파일을 계속 쓰지 않도록 갱신 표시를 새로 찍었습니다.

### 알아 두실 점

예전에 만든 **결과 링크**(경기를 그대로 다시 보는 링크) 중 울산·안양이 낀 경기는
다시 재생했을 때 결과가 달라질 수 있습니다. 두 구단의 선수 한 명씩(울산 이동경,
안양 문성우)의 특성이 이번에 함께 갱신됐기 때문입니다.
결과가 어긋나면 화면의 결과 지문이 서로 다르게 나오므로 모르고 넘어가지는 않습니다.
**초대 코드**(상대에게 건네 대전을 여는 코드)는 영향이 없습니다.

---

## v1.0 — 2026-08-13

첫 공개.

전술로 사람과 붙는 K리그 감독 게임입니다. 설치 없이 링크만 열면 됩니다.
개리그 매니저(KM26)의 매치엔진을 원저작자 허락을 받아 그대로 가져와,
사람끼리 전술로 붙는 것 하나만 남겼습니다. 시즌도 이적도 육성도 없습니다.

- K리그1·2 29개 구단 1,024명
- 라인업·전술을 짜서 대전 코드를 주고받으면 90분이 돌아갑니다
- 혼자 연습할 수 있는 컴퓨터 상대 세 단계
- 경기 2D 재생, 결정적 장면 다시 보기, 경기 뒤 팬 반응
