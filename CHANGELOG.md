# 변경 내역

새로 바뀐 것이 위에 옵니다.

판 번호(v1.0 같은 것)는 게임의 뼈대가 크게 달라질 때만 올립니다.
버그 수정이나 화면 손질은 번호를 올리지 않고 날짜별로 여기에만 적습니다.

---

## v1.0 이후 — 2026-08-14 (6차) · 폰에서 탭이 다 보입니다 · 첫 페이지 정리

### 화면

**위쪽 탭을 두 줄로 접었습니다 — 이제 여섯 개가 다 보입니다**

화면이 좁으면 왼쪽 바가 위쪽 탭으로 눕는데, 탭이 여섯 개라 폭을 넘쳐
**뒤쪽 탭(듀얼 기록실 · 선수단)이 늘 숨어 있었습니다.** 옆으로 밀면 나오긴 했지만,
PC 브라우저의 모바일 보기에서는 마우스로 끌어도 굴러가지 않아 **아예 닿을 수가
없었습니다.** 두 줄로 접으니 전부 보이고 밀 일도 없습니다.

**첫 페이지를 정리했습니다**

- 버튼 글이 길어 폰에서 두 줄로 접히던 것을 **짧게** 줄였습니다.
  `⚽ 라인업 짜기` 하나가 큰 버튼이고, 나머지 넷은 **두 칸 격자**로 나란히 놓았습니다.
- **전술 게시판 · 듀얼 기록실**로 첫 화면에서 바로 갈 수 있습니다.
- 세 걸음 설명의 2번을 **`51자 코드를 건넨다` → `듀얼을 한다`** 로 바꿨습니다.
  이제 코드를 주고받는 것 말고도 게시판에서 고르는 길이 있어서, 코드가 앞에 서는 것이
  맞지 않았습니다.
- 3번에 **기록실과 전술 승률** 이야기를 넣었습니다.

---

## v1.0 이후 — 2026-08-14 (5차) · 전술 게시판에 성적이 붙었습니다

### 새로 생긴 것

**전술 게시판의 라인업마다 승·무·패가 붙습니다**

듀얼 기록실에 쌓인 실제 경기로 매깁니다 — 따로 올릴 것도, 적을 것도 없습니다.
붙을 때마다 저절로 쌓입니다.

- **홈과 원정을 갈라서** 보여 줍니다. 같은 전술이라도 홈에서 밀어붙이는 것과
  원정에서 받아치는 것은 다른 이야기니까요.
- **성적이 좋은 라인업이 위로 옵니다.** 게시판을 열면 바로 그 순서입니다
  (`🕘 최신순`으로 바꿀 수 있습니다).
- 경기 화면에서 상대를 고를 때도 같은 순서로 뜨고, 줄에 **승률 뱃지**가 붙습니다.
  줄에 마우스를 올리면 홈·원정 전적이 나옵니다.

> **한 판 이긴 라인업이 9승 1패보다 위로 오지 않습니다.** 경기 수가 적으면 순위를
> 평균 쪽으로 당겨 줄을 세웁니다. 아직 아무도 안 붙어 본 라인업은 딱 평균 자리에
> 서므로, 새로 올린 글이 맨 아래로 가라앉지도 않습니다.

---

## v1.0 이후 — 2026-08-14 (4차) · 경기 화면 손질

### 화면

**해설이 여덟 배 촘촘해졌습니다**

2D 는 쉼 없이 움직이는데 글자는 한 분에 한 번 바뀌어 멈춘 것처럼 보였습니다.
알고 보니 엔진은 해설을 **두 갈래**로 내고 있었는데, 그중 촘촘한 쪽을 여태 한 줄도
쓰지 않고 버리고 있었습니다.

| 갈래 | 90분에 | 쓰임 |
|---|---|---|
| 문자중계 | 70~90줄 | 아래 기록창에 쌓이는 줄 (골·카드·교체 …) |
| **실시간 자막** | **600줄쯤** | 여태 버려지던 것 — `이승우, 한 박자 빠르게 때립니다!` |

이제 이 자막이 2D 바로 아래 한 줄에 흐릅니다. **화면이 그리는 그 시각에 맞춰** 나오므로
공보다 글자가 앞서지 않습니다 — `골!!` 자막이 미리 뜨는 일은 없습니다.
장면을 되감아 보여 줄 때는 자막도 함께 되감습니다.

> ⚠ **자막은 결정적 장면과 초반 3분에서만 흐릅니다.** 그 구간은 필드가 8배속으로
> 흐르지만, 나머지 관전 구간은 **75배속**(1분 = 0.8초)이라 자막 한 줄이 0.12초만에
> 지나갑니다 — 글자를 읽는 사이 화면은 경기 시계로 70초를 가 버립니다.
> 자막은 원래 8배속 하이라이트를 전제로 쓰인 문장들이라, 맞는 자리에서만 씁니다.
> 빠른 구간은 지금처럼 문자중계 줄이 담당합니다.

**`● 리플레이` 표시**

골 장면은 끝나고 한 번 더 느리게 돌아가는데 아무 표시가 없어 **같은 화면이 되풀이되는
고장처럼** 보였습니다. 필드 **오른쪽 위에 빨간 글씨**로 다시 보는 중임을 알립니다.

**전광판을 작게 줄였습니다**

점수 30px·패딩 16px 에 진행 막대까지 더해져 2D 필드에 견줘 자리를 너무 많이 차지했습니다.
주인공은 필드이므로 전체를 한 단계씩 줄였습니다 — 폰에서 필드 높이의 32% 입니다.

---

## v1.0 이후 — 2026-08-14 (3차) · 듀얼 기록실

### 새로 생긴 것

**🏛 듀얼 기록실** — 사람이 짠 전술끼리 붙은 경기가 **자동으로 남습니다.**

경기가 끝나면 결과 링크가 기록실에 올라갑니다. 목록에는 **누가(닉네임) 어느 구단으로
누구와 붙어 몇 대 몇이었는지**가 뜨고, `▶ 다시 보기`를 누르면 그 경기가 **토씨 하나
안 틀리고** 재생됩니다 — 경기의 씨앗이 대전 코드 두 장에서 나오기 때문입니다.

- **자동 라인업이나 연습 경기는 올라가지 않습니다.** 사람이 짠 전술끼리 붙은 것만 남깁니다.
- **같은 경기는 한 번만 남습니다.** 코드 두 장이 같으면 결과도 같으니까요.
- 닉네임은 전술 게시판에 올린 이름을 따라갑니다. 올린 적이 없으면 **익명**으로 남습니다 —
  라인업 화면에서 게시판에 한 번 올리면 그 이름이 계속 붙습니다.
- 닉네임·구단으로 찾을 수 있습니다(한 글자·초성도 됩니다).

> ⚠ 이 기능은 게시판 저장소에 표를 하나 더 만들어야 켜집니다
> (`docs/전술게시판-설정.md` 의 **2-1** 절). 만들기 전에는 기록실이 "아직 준비 중"으로
> 뜨고, 경기·게시판은 지금처럼 그대로 됩니다.

### 화면

**결과 링크를 열면 경기가 바로 시작됩니다**

`#m=…` 결과 링크는 **양쪽 라인업이 이미 다 정해진** 주소인데, 열면 대진 화면이 먼저 뜨고
`경기 시작`을 한 번 더 눌러야 했습니다. 고를 것이 하나도 없는데 한 번 더 누르는 것은
군더더기라 없앴습니다. 상대가 준 초대 링크(`#c=…`)는 그대로입니다 — 그건 내 라인업을
골라야 하니까요.

---

## v1.0 이후 — 2026-08-14 (2차) · 전술 게시판 화면 · 경기 화면 정리

판 번호는 그대로 v1.0 입니다.

### 새로 생긴 것

**🗂 전술 게시판이 제 화면을 갖췄습니다** — 왼쪽 바에 `전술 게시판` 이 생겼습니다.
올라온 라인업을 카드로 펼쳐 보고, **골라서 바로 붙습니다**.

- **찾기** — 닉네임과 구단으로 찾습니다. **한 글자만 넣어도** 바로 좁혀집니다
  (`안` → 안양). **초성**도 됩니다 (`ㅇㅇ` → 안양).
  글자를 칠 때마다 서버에 다시 묻지 않으므로 기다림이 없습니다.
- **자주 찾는 구단** — 지금 게시판에 올라와 있는 구단만 눌러 볼 수 있게 띄웁니다.
- 카드마다 **구단 색 · 포메이션**이 함께 보입니다 (대전 코드를 그 자리에서 풀어 읽습니다).
- `⚽ 이 라인업과 붙기` 를 누르면 경기 화면이 **그 코드를 든 채로** 열립니다.

경기 화면 안의 게시판 목록에도 같은 **찾기 칸**이 붙었습니다.

### 화면

**경기 중 필드 위를 전부 비웠습니다**

필드 위에 왼쪽 위 이름표(`12' 선방`) · 아래 자막 · 오른쪽 위 버튼 두 개가 얹혀 있었습니다.
두 가지가 문제였습니다.

1. 이름표가 **무슨 장면인지를 공이 닿기도 전에** 알려 줘 김이 샜습니다.
2. 필드보다 그 글자들이 먼저 눈에 들어온다는 제보를 받았습니다.

이제 **필드에는 아무것도 얹지 않습니다.** 무슨 일이 벌어졌는지는 2D 바로 아래 한 줄이
제때 말해 주고, `↺ 다시 보기` · `⏩ 장면 건너뛰기` 는 아래 버튼 줄로 내렸습니다
(볼 것이 있을 때만 나타납니다). 골이 들어간 **뒤에** 뜨는 득점자 패널만 그대로입니다.

**폰에서 크기를 필드에 맞췄습니다**

2D 필드는 화면 폭에 맞춰 함께 줄어드는데 글씨·버튼·전광판은 데스크톱 크기 그대로여서,
폰에서는 필드보다 주변이 더 커 보였습니다. 전광판 · 해설 · 버튼 · 대진 칩 · 경기 기록을
필드를 기준으로 함께 줄였습니다. 경기 화면과 전술 게시판 화면 둘 다 적용했습니다.

### 버그 수정

**부상 교체가 한 박자 늦게 들어오던 것**

부상으로 실려 나간 자리를 **다음 분이 되어서야** 채웠습니다. 교체 검토가 분이 바뀔 때
한 번만 돌기 때문입니다. 실측하면 72분 부상 → 73분 교체, 76분 부상 → 77분 교체로
**최대 1분을 열 명으로** 뛰었습니다. 그 사이에 결정적 장면이 걸리면 열 명이 한참 보입니다.

이제 **실려 나가는 그 순간** 채웁니다. 벤치가 비었거나 교체 카드를 다 썼으면 그대로
열 명으로 뜁니다 — 그건 규칙대로입니다.

> 퇴장은 원래대로 **교체하지 않습니다.** 열 명으로 뛰는 것이 축구 규칙입니다.
> 부상은 경기당 0.08명꼴로 드뭅니다(26경기에 2번). 자주 보이는 장면은 아닙니다.

**경기 화면 구단 칩에도 전력 별점이 보입니다**

라인업·선수단 화면에만 있던 구단 전력 별점을 **경기 화면 대진 고르기**에도 붙였습니다.
어느 화면에서 고르든 같은 별이 뜹니다 — 세 화면이 같은 계산 하나(`src/ui/teamstar.js`)를
씁니다. 전에는 선수단과 라인업이 같은 코드를 따로 들고 있었습니다.

**별점이 구단 칸 밖으로 삐져나오던 것**

선수단 화면에서 별 다섯 개와 숫자가 카드 폭(85px)을 **15px 넘겨** 숫자가 테두리 밖으로
나가 보였습니다. 라인업 화면도 같았습니다. 카드 안에서는 별을 작게 쓰고, 그래도 넘치면
잘라 내도록 했습니다.

**모든 해설이 2D 보다 뒤에 나옵니다**

골에만 걸려 있던 것을 **장면이 잡힌 모든 대목**으로 넓혔습니다. 전에는
`크로스바를 강타합니다!` 를 먼저 읽고 나서 그 슛을 보게 돼 거꾸로였습니다.
이제 골 · 페널티킥 · 퇴장 · 선방 · 결정적 기회 모두, 공이 실제로 그 일을 하는
순간에 해설이 나옵니다.

**⏸ 일시정지가 장면을 볼 때도 듣습니다**

장면을 트느라 시계를 멈출 때마다 버튼이 `▶ 재생` 으로 바뀌어 있었습니다.
그래서 **장면이 돌아가는 중인데 버튼은 "멈춰 있다"고 말하고**, 거기서 재생을 눌러도
이미 돌아가고 있으니 아무 일도 안 일어나 보였습니다.

이제 장면을 보는 동안에도 버튼은 `⏸ 일시정지` 이고, 누르면 **화면이 그 프레임에서
실제로 섭니다.** 다시 누르면 그 자리에서 이어집니다. 경기가 끝나면 재생·끝까지 버튼은
꺼집니다 — 누를 것이 없는데 눌리면 그것도 고장으로 보이니까요.

**"지금 막 벌어진 일" 한 줄을 필드에 붙였습니다**

필드보다 아래 재생 버튼 줄에 가까이 붙어 있어서 폰에서는 둘이 겹쳐 보였습니다.
필드에서 벌어진 일을 말하는 자막이므로 **필드 쪽**에 이어 붙이고, 버튼 줄과는 띄웠습니다.
기본은 **한 줄**이고 문장이 길면 두 줄까지만 늘어납니다.

**저장 슬롯은 누르면 바로 불러옵니다**

전에는 슬롯 카드를 누르면 **화면 아래쪽에** "불러오시겠습니까" 상자가 따로 떴습니다.
카드에서 멀리 떨어져 있어서, 폰에서는 눌러도 아무 일이 안 일어난 것처럼 보이고
엉뚱한 것이 튀어나온 것처럼 읽혔습니다.

이제 **누르면 그 자리에서 올라옵니다.** 무엇이 올라왔는지 한 줄로 알려 주고 전술판까지
끌어다 줍니다. `💾 덮어쓰기` 와 `✕ 지우기` 는 그대로 한 번 묻습니다 —
그건 **저장해 둔 것이 사라지는** 일이라서요.

**`📋 전술 게시판에서 상대 고르기` 를 위로 올렸습니다**

전에는 `🔗 대전 코드 붙여넣기` 상자 **안에** 들어 있어서, 코드 칸을 먼저 고른 사람에게만
보였습니다. 상대를 못 구한 사람이 가장 먼저 보아야 할 것이므로 라인업 목록보다 위로
올렸습니다. 목록에서 고르면 코드 칸으로 **알아서 넘어갑니다** — 전에는 코드만 들어가고
칸은 그대로여서 자동 라인업으로 경기가 시작될 여지가 있었습니다.

---

## v1.0 이후 — 2026-08-14

판 번호는 그대로 v1.0 입니다. 버그 수정입니다.

### 버그 수정

**포메이션을 바꾸면 대전 코드가 발급되지 않던 문제**
5-3-2 로 짜 두었다가 4-3-3 으로 바꾸는 식으로 포메이션을 옮기면, 화면에는 "선발 11명"
이라고 나오는데 코드 발급을 누르면 "○○ 자리가 비어 있습니다" 라며 막히는 경우가
있었습니다.

전술판은 스물다섯 자리를 모두 보여 주는데, 실제로 경기에 나가는 것은 그중 지금
포메이션이 쓰는 열한 자리뿐입니다. 그런데 화면이 그 구분을 하지 않아, 지금 포메이션에
없는 자리에도 선수를 세울 수 있었습니다. 그러면 사람 수는 열한 명이 맞지만 정작
필요한 자리가 비어 있어 코드를 만들 수 없었습니다.

**골 해설이 2D 장면보다 먼저 나오던 문제**
골이 들어가는 장면을 보기도 전에 해설로 "골!" 을 먼저 읽게 돼, 정작 볼거리인 2D 가
김이 새 버렸습니다. 전광판 점수도 장면보다 먼저 올라갔습니다.

이제 **골 해설은 공이 골망에 꽂히는 그 순간에 나옵니다.** 장면이 재생되는 동안에는
자막과 화면 위 이름표도 결과를 감춥니다 — 몇 분인지만 보이고 "골" 인지 아닌지는
들어가는 순간에 드러납니다.

건너뛰거나 끝까지 감기를 눌러도 붙들어 둔 해설은 빠짐없이 나옵니다.

### 새로 생긴 것

**전술 게시판 — 상대를 못 구해도 붙을 수 있습니다**

코드를 주고받는 것 자체는 잘 되는데, **상대를 구하는 일**이 번거로웠습니다.
게시판에 글을 따로 쓰고, 댓글을 기다리고, 링크를 눌러 페이지를 옮겨야 했으니까요.

이제 라인업 화면에서 `🔗 대전 코드 발급` 을 누르면 그 자리에 **`📋 전술 게시판에 올리기`**
가 함께 나옵니다. **닉네임만 적고 올리면 끝입니다.** 다른 사람은 경기 화면 원정 칸의
**`📋 전술 게시판에서 고르기`** 에서 목록을 열어 그 라인업을 바로 상대로 세웁니다.

- **상대가 접속해 있을 필요가 없습니다.** 남겨 둔 라인업과 붙는 것입니다.
- **페이지를 옮기지 않습니다.** 코드를 복사해 어디에 붙일 일도 없습니다.
- 올라가는 것은 **닉네임 · 한 줄 소개 · 구단 · 대전 코드** 뿐입니다.
  로그인도 개인정보도 없습니다.

> 게시판이 안 보인다면 브라우저가 옛 파일을 쓰고 있는 것입니다 —
> 강력 새로고침(`Ctrl+Shift+R`)을 해 보세요.

### 화면

**조건부 지시가 발동했는지 알 수 있게 했습니다**

경기 중에는 아무도 버튼을 못 누르므로 **조건부 지시가 유일한 개입 수단**인데, 발동해도
회색 글씨로 다른 해설에 섞여 흘러가 버려서 실행됐는지조차 알기 어려웠습니다.

- 발동한 순간 해설창에 **금색 카드**로 세워 눈에 걸리게 했습니다.
  해설이 아니라 "내가 미리 적어 둔 것이 실행됐다"는 표시입니다.
- 경기가 끝나면 **경기 기록 아래에 요약**이 붙습니다 — 몇 분에 어떤 지시가 발동했는지
  시간순으로 모아 보여 줍니다. 하나도 발동하지 않았으면 그렇다고 알려 줍니다.

> 조건이 맞았더라도 전술이 **이미 그 상태였다면** 바꿀 것이 없어 줄이 남지 않습니다.
> 예를 들어 압박이 이미 최대인데 "압박을 올린다"가 걸리면 아무 일도 일어나지 않습니다.

**2D 바로 아래에 "지금 막 벌어진 일" 한 줄**

눈은 필드에 가 있는데 해설은 아래쪽 기록에 쌓이니, 무슨 일이 벌어졌는지 보려면
시선을 아래까지 내려야 했습니다.

이제 **2D 화면 바로 아래에 한 줄**이 붙어 가장 새 소식만 보여 줍니다. 바뀔 때 한 번
번쩍이므로 놓치지 않습니다. 아래 기록은 지금까지처럼 그대로 쌓입니다 — 두 개가 함께 있습니다.

**경기 시작 직후가 죽은 화면처럼 보이던 것**

경기 초반에는 결정적 장면이 없어서 2D 가 아주 빠르게 흘러갔고, 해설도 몇 줄 없는데
상자 **아래쪽**에 붙어 있어서 화면 위쪽이 통째로 비었습니다. 그래서 시작하자마자
"멈춰 있는 검은 화면"처럼 보였습니다. 두 가지를 함께 고쳤습니다.

- **처음 3분은 빨리 감지 않습니다.** 결정적 장면을 보여 줄 때와 같은 속도로, **같은 밝기**로
  흐릅니다(`▶ 경기 시작`). 경기가 어떻게 시작하는지를 보고 나서 나머지가 빨라지고,
  그때부터 화면이 어두워지며 `○ 경기 진행 중` 으로 바뀝니다 — 어두운 것은
  "지금은 빨리 감는 중"이라는 표시입니다.
- **해설이 위에서부터 찹니다.** 새 줄이 맨 위에 붙고 지나간 줄이 아래로 밀려 내려갑니다.
  예전에는 아래에서 위로 쌓여서 초반에 위쪽이 비어 보였습니다.

전체 재생 시간은 90분 기준 1분 12초에서 **1분 30초쯤**으로 늘어납니다.
급하시면 2·4배속이나 `⏭ 끝까지`를 쓰시면 됩니다.

**선발 명단 정렬**
자리 이름표(ST · LAM 처럼 글자 수가 다른 것)의 폭을 고정해, 선수 이름과 아래 역할 칸이
줄마다 같은 자리에서 시작하도록 맞췄습니다.

**전술판에서 쓰지 않는 자리가 구분됩니다**

지금 포메이션이 쓰지 않는 자리는 **흐리게** 보이고 선수를 세울 수 없습니다.
누르거나 끌어다 놓으면 **어느 포메이션에서 그 자리가 열리는지** 알려 줍니다
(예: "4-3-3 에는 LM 자리가 없습니다 — 3-4-2-1 · 3-4-3 · 4-1-4-1 에서 열립니다").
그 칸에 손을 얹어도 같은 안내가 보입니다.

어느 포메이션도 쓰지 않는 `SW` 칸은 전술판에서 아예 없앴습니다.

저장해 둔 라인업을 불러올 때, 그 자리들을 모두 담는 포메이션이 있으면 그쪽으로 맞춰 줍니다.

**골키퍼의 선호 포지션이 엉뚱하게 나오던 문제**
골키퍼인데 선호 포지션이 중앙 미드필더나 공격수로 표시되는 선수가 **열 명** 있었습니다
(안양 김정훈, 울산 류성민, 전북 이한결·정대영, 포항 홍성민, 서울 윤기욱, 제주 허재원,
인천 왕민준, 광주 김동화, 부천 김찬영).

선호 포지션 표를 **이름으로만** 찾는데, 실제 등록 명단에 같은 이름의 다른 선수가 있으면
그 사람의 자리가 골키퍼에게 넘어오고 있었습니다. 이제 골키퍼는 골키퍼만, 필드 플레이어는
골키퍼가 아닌 자리만 선호로 갖습니다.

반대 경우도 하나 있어 함께 고쳤습니다 — 포항 김호진 선수는 수비수인데 골키퍼를 선호로
들고 있어서 골문 앞에 세워도 아무 문제가 없는 것처럼 보였습니다.

### 알아 두실 점

선수 데이터가 바뀌었지만 **명단·능력치는 하나도 달라지지 않았습니다.** 그래서 지금까지
나눠 가진 **대전 코드는 그대로 쓸 수 있습니다.** 예전 코드를 넣으면 화면이 "옛 판 코드"
라고 알려 주고 그대로 읽습니다.

**결과 링크**는 포항이 낀 경기만 결과가 달라질 수 있습니다(김호진 선수의 소화 가능 자리가
바뀌었기 때문입니다). 나머지 구단은 영향이 없습니다.

---

## v1.0 이후 — 2026-08-13 (두 번째)

판 번호는 그대로 v1.0 입니다. 버그 수정입니다.

### 버그 수정

**결과 링크를 열면 전혀 다른 경기가 재생되던 문제**
같은 링크인데 열 때마다 점수도 득점자도 달랐습니다. 경기의 무작위 씨앗을 대전 코드가
아니라 화면이 들고 있던 라인업에서 뽑고 있었던 탓입니다. 화면에서 짠 라인업과 코드에서
되살린 라인업은 속이 조금 달라서, 같은 라인업인데도 씨앗이 갈렸습니다.
이제 두 사람이 실제로 주고받는 대전 코드에서 씨앗을 뽑습니다 — 코드가 같으면 언제
어디서 열어도 같은 경기가 나옵니다.
경기 중 **끝까지 버튼은 이 문제와 무관**했습니다. 경기는 미리 다 계산해 두고 화면은
재생만 하므로, 얼마나 보다가 눌러도 결과가 바뀌지 않습니다. 버튼은 그대로 둡니다.

**교체 명단 번호(S1~S9)가 아무 의미가 없던 문제**
화면에는 번호를 붙여 놓고 실제로는 능력치 순으로 아무나 들어갔습니다.
이제 번호가 곧 투입 우선순위입니다. 먼저 쓰고 싶은 선수를 위로 올려 두세요.

**지고 있을 때의 교체가 팀을 더 약하게 만들던 문제**
지고 있으면 수비수를 빼고 공격수를 넣었는데, 교체 선수는 나간 선수의 자리를 그대로
물려받기 때문에 스트라이커가 센터백 자리에 서 있었습니다. 수비는 무너지고 공격은
늘지 않았습니다. 이제 그 자리를 실제로 소화할 수 있는 선수 중에서 고릅니다.

**부상으로 빈 자리가 끝까지 안 채워지던 문제**
선수가 다쳐 나가도 아무도 들어오지 않아 남은 시간을 열 명으로 뛰었습니다.
이제 부상으로 자리가 비면 시간과 상관없이 곧바로 채웁니다.

### 화면

**교체 명단에 자동 교체 규칙 안내 추가**
언제(후반 여섯 번), 누구를(카드·체력·점수 상황), 어떤 순서로(번호 순) 바꾸는지를
라인업 화면 교체 명단 위에 적었습니다. 골키퍼는 자동 교체하지 않습니다.

### 알아 두실 점

씨앗을 뽑는 방식이 바뀌었으므로, **이전에 만든 결과 링크는 그때와 다른 경기를 보여
줍니다.** 지금까지는 어떤 경로로 들어오느냐에 따라 결과가 갈렸으니 애초에 고정된 값이
없었습니다. 지금 만드는 링크부터는 항상 같은 경기가 재생됩니다.

---

## v1.0 이후 — 2026-08-13

판 번호는 그대로 v1.0 입니다. 버그 수정과 화면 손질입니다.

### 버그 수정

**왼쪽·오른쪽 풀백이 전부 중앙 수비수로 나오던 문제**
측면 수비수로 뛰어야 할 선수들이 화면에 죄다 중앙 수비수로 표시되고 있었습니다.
선수 132명의 선호 자리를 실제 뛰는 자리로 바로잡았습니다.

**업데이트 뒤에 예전 대전 코드가 안 읽히던 문제**
위 수정으로 선수 데이터가 바뀌면서, 그 전에 발급해 주고받은 대전 코드가 전부
막혔습니다. 확인해 보니 선수 명단 자체는 그대로였기에, 예전 코드도 계속 쓸 수
있도록 열었습니다. 예전 코드로 들어오면 화면에 그 사실을 알려 줍니다.

**연습 경기 뒤에 결과 공유 링크 칸이 사라지던 문제**
연습 경기는 상대를 컴퓨터가 그 자리에서 짜기 때문에 원래 링크를 만들 수 없는데,
아무 설명 없이 칸만 사라져서 기능이 없어진 것처럼 보였습니다.
이제 왜 링크가 없는지와 사람과 대전하는 방법을 그 자리에 알려 줍니다.

### 화면

**구단 전력을 별점으로 표시**
라인업 화면과 선수단 화면에서, 구단을 눌러 보기 전에도 그 팀이 얼마나 강한지
별 다섯 개로 바로 확인할 수 있습니다.
K리그1 열두 팀이 모두 비슷한 별로 몰리지 않도록 팀끼리 비교하기 좋은 눈금을 씁니다.

**전술 화면에서 내부 수치 감추기**
전술 조절 막대 설명에 개발하면서 적어 둔 테스트용 숫자가 그대로 보이고 있어서
화면에서 없앴습니다. 무엇을 얻고 무엇을 잃는지에 대한 설명은 그대로 있습니다.

### 그 밖에

- 선수 데이터를 만드는 도구가 선호 자리 표를 빠뜨리던 것을 고쳐, 다시 만들어도
  같은 문제가 나지 않게 했습니다.
- 접속하는 기기가 예전 파일을 계속 쓰지 않도록 갱신 표시를 새로 찍었습니다.

### 알아 두실 점

예전에 만든 **결과 링크**(경기를 그대로 다시 보는 링크) 중 울산·안양이 낀 경기는
다시 재생했을 때 결과가 달라질 수 있습니다. 두 구단의 선수 한 명씩(울산 이동경,
안양 문성우)의 특성이 이번에 함께 갱신됐기 때문입니다.
결과가 어긋나면 화면의 결과 지문이 서로 다르게 나오므로 모르고 넘어가지는 않습니다.
**초대 코드**(상대에게 건네 대전을 여는 코드)는 영향이 없습니다.

---

## v1.0 — 2026-08-13

첫 공개.

전술로 사람과 붙는 K리그 감독 게임입니다. 설치 없이 링크만 열면 됩니다.
개리그 매니저(KM26)의 매치엔진을 원저작자 허락을 받아 그대로 가져와,
사람끼리 전술로 붙는 것 하나만 남겼습니다. 시즌도 이적도 육성도 없습니다.

- K리그1·2 29개 구단 1,024명
- 라인업·전술을 짜서 대전 코드를 주고받으면 90분이 돌아갑니다
- 혼자 연습할 수 있는 컴퓨터 상대 세 단계
- 경기 2D 재생, 결정적 장면 다시 보기, 경기 뒤 팬 반응
