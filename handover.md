지금까지의 대화 전체를 분석해서 HANDOVER.md 파일을 프로젝트 루트에 생성해줘.

처음에 `git status`를 확인해서 작업 트리 상태(클린 / 미커밋 변경 있음)를 파악할 것.

\*\*작성 시작 전 — 아래 두 단계를 TodoWrite 체크리스트로 만들어 하나씩 완료하며 진행할 것 (읽고 넘기지 말고 실제로 실행):\*\*

\*\*1단계 (git status)\*\* — 위에서 확인.
\*\*2단계 (결정 전파 검증 — 필수 실행)\*\* — 이번 세션에서 바뀐 \*\*수치·방침·기준\*\*(글자수·말속도·해상도·모델·파일위치 등)을 먼저 나열하고, 각 항목을 Grep 도구로 전 문서·코드에서 \*\*실제로 검색\*\*해 구버전 잔재가 없는지 확인한다(§결정 전파 검증 규칙). 잔재가 나오면 그 자리에서 정정하거나 `[미반영]`로 다음 단계에 올린다. \*\*이 grep 실행 흔적이 없으면 HANDOVER를 작성하지 말 것.\*\*

다음 항목을 포함:

\## 세션 메타데이터
\- 날짜, 작업 브랜치, 마지막 commit (해시 + 한 줄 요약)
\- git 상태 (클린 / 미커밋 변경 N개 — 있다면 어떤 것)

\## 작업 요약
\- 이번 세션에서 작업한 내용과 완료된 사항
\- (이미 git log / TODO.md / docs에 있는 세부는 중복 작성 X, 참조만)

\## 성공/실패 기록
\- 어떤 접근이 통했고, 어떤 버그를 어떻게 해결했는지
\- 시도했지만 실패한 방법과 그 이유

\## 주요 결정 사항
\- 내린 결정과 그 이유 (왜 A 대신 B를 택했는지)
\- \*\*각 결정에 "반영 위치 + 미반영분" 명시\*\* — 결론만 적지 말고, 그 결정을 어느 문서/코드에 반영했는지와 아직 안 박힌 곳은 어디인지 함께 적는다 (아래 §결정 전파 검증 규칙).

\## 주의사항 \& 교훈
\- 다음 세션에서 주의할 점, 반복하면 안 되는 실수

\## 다음 단계
\- 이어서 해야 할 작업 목록 (우선순위 포함)
\- \*\*맨 위에 `[미반영]` 항목\*\*(2단계 결정 전파 검증에서 발견한 — 결론은 났으나 문서·코드에 아직 안 박힌 것)을 올린다
\- 가능하면 TODO.md의 항목 번호로 참조

\## 보류 / 백로그
\- \*\*"다음 단계(이어서 할 일)"와 분리\*\*해서, 결정이 보류됐거나 아직 착수 안 한 항목을 따로 적는다 (다음 단계에 묻혀 다음 세션에서 사라지지 않게)
\- 각 항목에 \*\*보류 사유 + 재개 조건\*\*을 한 줄로 (예: "Google batch — billing+24h 비동기 부담으로 보류, 대량·저비용 필요 시 재검토")

\## 중요 파일 맵
\- 이번 세션에서 주로 다룬 파일 경로와 역할
\- 미완성 작업이 어느 파일 어느 위치에 있는지

\## 다음 세션 부트스트랩
파일 마지막에 다음 형식으로 출력:
> 다음 세션 시작 시 다음 입력으로 컨텍스트 복원:
> `@HANDOVER.md 읽고 이어서 작업 진행`

\---

\## 구체적 식별자 검증 규칙 (필수)

\*\*HANDOVER에 적는 모든 "구체적 식별자(specific identifier)"는 작성 직전에 ground-truth로 1회 검증할 것.\*\* 검증 없는 추측은 다음 세션에서 그대로 신뢰되어 잘못된 SQL/명령을 실행하게 만든다 (실측 사례: `fortunes.status` — fortunes 테이블에 status 컬럼은 존재한 적 없음. orders 테이블 컬럼이었는데 schema 직접 확인 없이 직관으로 작성됨).

검증 대상 식별자:
\- SQL 쿼리에 적힌 \*\*테이블명·컬럼명·enum 값\*\* → migration 파일 또는 sqlc generated 파일로 확인
\- HTTP 엔드포인트 \*\*경로 / 메서드 / 요청 본문 키\*\* → 라우터 / 핸들러 소스로 확인
\- CLI 명령에 적힌 \*\*앱 이름 / 시크릿 키 / 플래그\*\* → fly.toml / vercel.json / 실제 환경변수 명세로 확인
\- 파일 경로·함수명·환경변수 이름 → 실제 파일·grep으로 확인

검증 방법:
\- 각 식별자에 대해 정확히 한 번 Read / Grep으로 ground-truth 확인. 검증 흔적이 한 번도 없는 식별자는 HANDOVER에 인라인하지 말 것.
\- 검증 후 명령·SQL 끝에 근거 한 줄 인용 권장: `-- schema: api/internal/db/migrations/000002_init_schema.up.sql L66-79`

검증을 못한 경우 처리:
\- 추상도를 한 단계 낮춰서 적기 (예: `SELECT id, status FROM fortunes` → `fortunes 최근 row 5건 — 컬럼은 \\d fortunes로 확인 후 SELECT`)
\- 또는 `[검증 필요]` 마커를 명시 (예: `# [검증 필요] 컬럼명 다음 세션 시작 시 확인`)

\---
\## 결정 전파 검증 규칙 (필수)
\*\*이번 세션에서 내린 판단·방침·수치 변경은 "결론"만 적지 말고, 그 결론이 관련 문서·코드에 실제 반영됐는지 grep/Read로 확인할 것.\*\* 한 곳만 고치고 다른 곳에 구버전이 남는 "부분 반영"이 흔하며, 다음 세션은 남은 구버전을 그대로 신뢰해 잘못된 답을 낸다 (메모리·컨텍스트의 결론과 실제 문서가 어긋나는 동기화 갭).
\*\*실측 사례\*\*: 직전 세션에서 "3,000자는 fallback, 15분 = 7,500자"로 결론냈으나 `longform-script-standard.html` §3 본문만 고치고 체크리스트엔 "2,700~3,300자"가 잔존 → 다음 세션 시작 시 "3,000자"로 오답, 코드와 비교검증한 뒤에야 문서 누락을 발견했다.
검증·기록 방법:
\- 세션 중 바뀐 \*\*수치·방침·기준\*\*(글자수·말속도·해상도·모델·파일위치 등)을 모두 나열한다.
\- 각각을 \*\*grep으로 전 문서·코드에서 검색\*\*해 구버전 값이 남아있지 않은지 확인한다 (결론을 낸 곳 한 군데가 아니라 전 범위).
\- 반영 완료 = 결정에 `반영: <파일>:<위치>` 한 줄 인용. 부분/미반영 = `[미반영] <남은 구버전 위치>`를 "다음 단계" 최상단에 올린다.
\- 단일 출처(SSOT) 문서가 있으면(이 프로젝트: `docs/production-spec.html`) 거기에 먼저 반영하고 다른 문서는 그 링크를 가리키게 해, 한 곳만 고치면 되는 구조로 만든다.
\---
추가로 — \*\*사용자 선호 / 반복 패턴 / 주의사항\*\* 중 다음 세션에서도 알면
좋을 것이 있으면 사용자의 memory 폴더 (auto memory 시스템)에 적절한
type의 memory 파일로 저장해줘.

\## 진행상태 메모리 저장 (필수 — HANDOVER.md는 세션마다 덮여 사라진다)
HANDOVER.md만으론 부족하다: 다음 세션이 HANDOVER를 못 읽거나(clear 후 다른 작업 먼저),
두 번째 handover가 덮으면 진행상태가 증발한다. 따라서 \*\*현재 진행상태를 project 타입
memory 파일에도 반드시 반영\*\*한다 (HANDOVER와 별개, 영구 보관소).

저장/갱신할 내용:
\- \*\*현재 어디까지 했나\*\* (완료 단계) + \*\*지금 멈춰있는 지점\*\* (blocking 상태: 예 "11편 이미지 검수 대기")
\- \*\*다음 할 일 최상위 1~3개\*\* (미착수 포함 — 예 "i2v 검수 UI 미착수")
\- 이번 세션에서 새로 박제한 \*\*방침·게이트·규칙\*\*의 요지 + 정본 위치
\- HANDOVER.md를 정본으로 가리키는 한 줄 (`정본 상세=HANDOVER.md(날짜)`)

방법:
\- 기존 진행 메모리 파일이 있으면(예 `project-*-backlog.md`, `project-*-progress.md`) \*\*그 파일을 갱신\*\* (새 파일 남발 금지, 중복 확인).
\- 없으면 `project` 타입으로 신규 생성 + `MEMORY.md` 인덱스에 한 줄 등재.
\- 상대 날짜는 절대 날짜로 변환해 기록.
\- 저장 후 grep으로 \*\*실제 반영됐는지 1회 확인\*\* (진행상태 키워드가 파일에 있는지).

제외할 것:
\- git log로 알 수 있는 commit 세부 (해시·메시지만 참조)
\- 도구 출력 그대로 인용 (컨텍스트 + 결정만)
\- TODO.md / README.md / docs/\* 에 이미 있는 내용 (링크로만)
\- \*\*검증 안 된 추측 명령·SQL·경로\*\* — "이 정도면 맞겠지" 모드로 인라인 금지 (위 §구체적 식별자 검증 규칙 참조)

마무리에 출력:
\- HANDOVER.md 작성 완료 알림
\- 다음 세션 시작 명령 안내 한 줄

파일명 기본은 HANDOVER.md.
날짜별로 관리하고 싶으면 HANDOVER-YYYY-MM-DD.md 형식도 가능.
