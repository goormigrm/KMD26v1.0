# KMD26 · 개리그 매니저 듀얼

사용자끼리 **전술로 대전**하는 축구 감독 게임입니다.
서버가 없습니다. 라인업을 코드로 만들어 링크로 주고받으면, 양쪽 브라우저에서 **완전히 동일한 경기**가 재생됩니다.

```
[A] 라인업 작성 → 초대 링크 게시판에 올림
[B] 링크 클릭 → 자기 라인업 작성 → 경기 관람 → 결과 링크를 A에게
[A] 결과 링크 열기 → 토씨 하나 안 틀리고 같은 경기
```

> 🚧 **개발 중입니다.** 단계 0~8 · A 를 마쳤습니다 — 라인업을 짜고, 코드를 주고받아 대전하고,
> 혼자 연습하고, 조건부 지시까지 적을 수 있습니다. **v1.0 으로 오픈베타 공개 준비를 마쳤습니다.**
> 단계 9(밸런스 검증)는 사용자 결정으로 건너뜁니다 — 오픈베타 피드백으로 잡습니다.

> ## 🔧 고치기 전에 [CONTRIBUTING.md](CONTRIBUTING.md) 를 먼저 보세요
>
> **PC 두 대에서 번갈아 작업합니다.** 한쪽에서 빠뜨리면 다른 쪽에서 조용히 깨집니다.
> 다른 PC 가 밀어 둔 것을 받는 법, 자동 생성 파일, **버전 도장 찍는 순서**,
> 선수 데이터를 고칠 때 옛 대전 코드를 살리는 법, 커밋 전 확인 목록이 거기 있습니다.
> 이 문서(DEVELOPMENT.md)는 **왜 그렇게 만들었는가**를 적은 개발 기록입니다.

## 확인 페이지

**<https://goormigrm.github.io/KMD26v1.0/>** — 서버를 띄울 필요 없이 바로 열립니다.

| 페이지 | 확인하는 것 |
|---|---|
| [단계 2 — 결정론](https://goormigrm.github.io/KMD26v1.0/test/determinism.html) | 같은 시드로 100번 돌려 경기가 완전히 같은가 |
| [단계 3 — 전술 반영](https://goormigrm.github.io/KMD26v1.0/test/tactics.html) | 전술 슬라이더 8종이 경기를 실제로 바꾸는가 |
| [단계 1 — 헤드리스 구동](https://goormigrm.github.io/KMD26v1.0/test/headless.html) | 엔진이 화면 없이 90분을 완주하는가 |

오프라인으로 개발할 때만 로컬 서버가 필요합니다 (ES 모듈이라 파일을 그냥 열면 안 됩니다).

```bash
python -m http.server 8000
```

---

## 단계 2 — 결정론

<https://goormigrm.github.io/KMD26v1.0/test/determinism.html> 을 열고 세 버튼을 눌러 보세요.

| 버튼 | 확인하는 것 | 통과 조건 |
|---|---|---|
| **같은 시드 100회 검증** | 같은 시드로 100번 돌려 결과 지문이 하나인가 | 서로 다른 지문 **1종** |
| **다른 시드 10개 비교** | 시드가 다르면 경기도 다른가 | 서로 다른 지문 **10종** |
| **Math.random 누출 검사** | 시드 밖 난수가 남아 있는가 | 예외 없이 완주 |

결과 지문은 **스코어 · 득점 시각 · 해설 전문 · 슈팅/파울/카드**를 전부 합쳐 만든 해시입니다.
지문이 같으면 두 사람이 본 경기가 토씨 하나까지 같다는 뜻입니다.

> ⚠️ 100회는 **약 14분** 걸립니다 (판당 8초). 화면이 멈추지 않도록 10판씩 끊어서 돌립니다.

**2026-08-13 전수 검증 통과** — 서로 다른 지문 1종 · 10개 시드 전부 다른 결과 · `Math.random` 누출 0건.

---

## 단계 3 — 전술 반영 검증

<https://goormigrm.github.io/KMD26v1.0/test/tactics.html>

전술 슬라이더를 극단으로 옮겼을 때 경기가 실제로 달라지는지, 기대한 방향으로 달라지는지 봅니다.

| 버튼 | 확인하는 것 | 통과 조건 | 경기 수 |
|---|---|---|---|
| **① 반영 여부만 즉시 확인** | 엔진이 그 값을 읽기는 하는가 | 9종 모두 **지문이 달라짐** | 18판 |
| **② 전체 항목 측정** | 기대한 방향으로 움직이는가 | 항목별 **75% 이상**의 쌍이 같은 방향 | 16쌍 × 9항목 = 288판 |
| **③ 한 항목만** | 위와 같음 | 위와 같음 | 32판 |

같은 시드로 짝을 지어 낮은 값·높은 값을 나란히 돌리기 때문에, 적은 표본으로도 차이가 드러납니다.

> ⏱ **경기 한 판이 약 8초입니다.** 13,500틱 동안 22명이 각자 판단하니 원래 무거운 일이고,
> 줄일 수 있는 종류의 비용이 아닙니다. 대신 판이 서로 독립이라 **Web Worker 로 코어를 나눠 씁니다**
> (설계서 §1-2). 화면에 남은 시간이 실시간으로 표시됩니다.
> 코어를 8개 쓰면 ①은 30초 안쪽, ②는 5~8분 정도입니다.

> 📌 **승률로 판정하지 않습니다.** 설계서의 "승률 3%p" 기준을 채우려면 항목당 수천 경기가 필요한데
> 브라우저가 감당할 양이 아닙니다. 승률 기준 판정은 **오픈베타의 실제 플레이**로 넘겼습니다
> (단계 9 건너뛰기 — 2026-08-13 사용자 결정).
> 대신 슬라이더가 직접 건드리는 지표(압박 → 태클 수 등)를 봅니다.

### 연결 여부 — 9종 전부 확인 완료 (2026-08-13)

`tools/simcheck` 으로 같은 시드에 슬라이더만 바꿔 돌리고 결과 지문을 비교했습니다.

| 항목 | 결과 | 직접 지표 |
|---|---|---|
| **패스 길이** | ❌ 미반영 → ✅ **고침** | 롱패스 비율 0.4% → 11.1% · 평균 패스 14.7m → 20.9m (90분 3쌍) |
| 템포 | ✅ | 패스 수 |
| 압박 | ✅ | 태클 시도 |
| 수비 라인 | ✅ | 상대 오프사이드 |
| 폭 | ✅ | 크로스 |
| 멘탈리티 | ✅ | 슈팅 |
| 태클 | ✅ | 파울 |
| 중거리 슛 | ✅ | 중거리 슛 (90분 2쌍) |
| 역습 | ✅ | 패스 수 |

> ⚠️ **짧은 경기로는 놓칠 수 있습니다.** 15분 표본에서는 중거리 슛의 기본(2)과 적극(4)이
> 같은 지문을 냈습니다. 판단이 뒤집힐 기회가 그 안에 없었을 뿐이고, 정규 90분에서는 갈립니다.
> 연결 여부를 의심할 때는 반드시 `-secs 5400` 으로 다시 보세요.

### ⭐ 측정으로 찾은 것 — "패스 길이"가 미반영이었습니다

같은 시드로 `pass=0`(짧게)과 `pass=4`(길게)를 돌렸더니 결과 지문이 **한 글자도 같았습니다.**
경기가 완전히 동일하다는 뜻입니다.

커널에서 `T.pass` 를 참조하는 곳은 `tacticSig()` — 전술이 바뀌었는지 감지하는 서명 문자열 하나뿐이고,
경기 계산에는 한 번도 쓰이지 않았습니다. `longGate`("몇 m부터 길게 차는가")도 선수 특성만 읽고
팀 지시는 안 봤습니다. **화면에 슬라이더를 띄워 놓고 아무 일도 안 일어나면 그건 거짓말입니다.**

| id | 어디에 | 무엇을 |
|---|---|---|
| `PASS-01` | `evaluatePassOptions` | 짧게면 거리 부담↑·전진 이득↓, 길게면 반대 — **목표 선택**이 달라진다 |
| `PASS-02` | 위 함수의 점수식 | 위 계수를 실제로 곱한다 |
| `PASS-03` | `longGate` | 길게 차는 문턱을 팀 지시로도 움직인다 — **차는 모양**이 따라온다 |

난수를 더 쓰지 않으므로 결정론에는 영향이 없습니다.

> 📌 v1 설계서의 "역습·템포가 2D 엔진에 미반영"은 사실이 아니었지만
> (`tempoK()`·`counterOn()` 둘 다 살아 있습니다), **같은 종류의 문제가 다른 슬라이더에 있었습니다.**
> "있다"를 확인하는 것과 "전부 있다"를 확인하는 것은 다릅니다.

### 측정 뒤 고친 것 (16쌍 · 2026-08-13)

| 항목 | 수정 전 | 조치 | 16쌍 재측정 |
|---|---|---|---|
| 압박 | 태클 시도 17.4 → 17.4 (5/16) | `PRESS-01` `PRESS-02` | ✅ **+44% · 13/16 뚜렷** |
| 템포 | 패스 수 +5.4% (11/16 약함) | `TEMPO-01` | ✅ **+13% · 14/16 뚜렷** |
| 멘탈리티 | 슈팅 14.3 → **12.3** (거꾸로) | `MENT-01` | ❌ **여전히 거꾸로** (-10%, 6/16) |
| 폭 | 크로스 +16% (9/16 잡음) | ⛔ **보류** | ❌ 그대로 (9/16) |

> ⚠️ **멘탈리티는 못 고쳤습니다.** 3쌍 표본에서 좋아진 것처럼 보였으나(15.3→18.3)
> 16쌍에서는 여전히 뒤집혀 있습니다. **적은 표본으로 "고쳤다"고 판단하면 안 됩니다.**
> `MENT-01` 은 방향이 맞으므로 켜 둔 채로 두었습니다 — 끄면 더 나빠집니다.

압박은 진짜 배선 오류였습니다. `pressureOn` 이 **압박당하는 쪽**의 지시로 계수를 계산했고,
`tryTackle` 은 `T.press` 를 아예 안 읽었습니다. 압박을 올려도 더 달려들지 않았던 것입니다.

### ⛔ 남은 과제

**폭(width)** — 넓게 서면 오히려 크로스가 줍니다. 문턱에 팀 폭을 넣어 봤더니
(`WIDTH-01`) 10쌍에서 11.6 → 8.5 로 **더 나빠져서 꺼 두었습니다.**
넓게 서면 올릴 기회는 늘지만 **박스 안에 댈 사람이 줄어**(`inBoxMates` 가 비면 크로스 취소)
두 힘이 맞섭니다. 손볼 곳은 문턱이 아니라 "박스에 누가 들어가는가"입니다.

**템포** — 계수 이월 버그는 고쳤지만 여전히 약합니다. 볼 보유 시간만 건드려서는 한계가 있고,
패스 선택 자체에 연결해야 할 것으로 봅니다.

수정안은 지우지 않고 `tools/patch_kernel.py` 에 `off=True` 로 남겨 두었습니다 —
측정 근거와 다음에 볼 곳까지 주석에 있습니다. 자세한 내용은 설계서 "남은 과제" 참고.

### 태클 억제책 (설계 결정 D-3)

태클 슬라이더 상한 4는 그대로 두고, 거칠게 갔을 때의 대가를 만들었습니다 — `src/engine/rules.js`.

| 억제책 | 구현 | 상태 |
|---|---|---|
| 누적 파울 엄격화 | 8회 ×1.15 · 12회 ×1.35 · 16회 ×1.60 | ✅ |
| 퇴장 시 팀 체력 소모 | 퇴장 1명당 +12% | ✅ |
| 심판 성향 비공개 | 킥오프 시점에만 공개 | ⬜ 화면 규칙이라 단계 6·7에서 |

---

## 단계 1 — 엔진 헤드리스 구동

<https://goormigrm.github.io/KMD26v1.0/test/headless.html> 를 열고 **경기 1판 돌리기**를 누르세요.

검토 기준 6가지가 자동으로 표시됩니다.

| 확인 항목 | 통과 조건 |
|---|---|
| 엔진이 예외 없이 완주 | `done === true` |
| 경기 시계 90분 도달 | 5,400초 |
| DOM 참조 없이 실행 | 모듈 로드 성공 |
| 이벤트 기록 | 1건 이상 |
| 슈팅 발생 | 1회 이상 |
| 심판 배정 | 이름 표시 |

> ⚠️ 이 단계의 스쿼드는 **합성 데이터**입니다. 실제 선수 능력치는 단계 4에서 붙입니다.

> 📌 **연속 실행 버튼은 두지 않습니다.** 듀얼은 같은 라인업이면 한 판만 성립하는 게임이라
> (시드가 양쪽 코드에서 나오므로) 반복 실행은 제품 개념과 맞지 않습니다.
> 대량 시뮬이 필요하면 `tools/realmatch -series` 로 돕니다 (개발 전용).

---

## 구조

```
index.html     첫 페이지 — 여기서 라인업으로 들어간다
lineup.html    라인업 · 전술 · 역할 · 조건부 지시 · 저장 슬롯 · 대전 코드 발급
match.html     경기 — 해설 재생 · 2D 장면 · 득점자 패널 · 팬 반응 · 연습 AI · 코드 입력
squad.html     선수단 — 29개 구단 1,024명
presets/       추천 라인업 파일 (화면 버튼은 v1.0 공개 때 뺐다 — 파일만 남아 있다)
src/engine/
  kernel.raw.js  KM26 매치엔진 추출본 — 원본 그대로 (자동 생성)
  kernel.js      위에 듀얼 패치를 입힌 것 — 엔진이 실제로 쓰는 파일 (자동 생성)
  rng.js         시드 난수 · 코드 두 개에서 시드 유도 · 누출 검사
  stubs.js       엔진이 참조하는 전역 문맥 스텁 (G · famK · refBias 등 5개)
  rules.js       듀얼 고유 규칙 — 커널을 감싼다 (D-3 태클 억제책 · AI 전술 변경)
  orders.js      조건부 지시 (단계 8) — 분 경계에서 판정한다
  replay.js      2D 장면 수집 · 킥오프 한 장 · 도움 이름 받아 적기
  reactions.js   경기 뒤 팬 반응 (우리 팬 · 타팬 · 상주 악플러 · 안양팬)
  teams.js       JSON → 엔진 팀 · 자동/AI 라인업 · 같은 구단 대전 · 라인업 지문
  duel.js        듀얼 경기 구동부 (createMatch 대체 + 헤드리스 실행)
  matchworker.js 경기 일꾼 — 한 판 8초라 화면에서 못 돌린다
src/codec/
  duelcode.js    대전 코드 (단계 5) — 300비트 → Base64url 51자 · 초대/결과 링크
tools/
  extract_engine.py   추출기  — 원본 index.html → kernel.raw.js
  patch_kernel.py     패치기  — kernel.raw.js → kernel.js
  extract_data.py     선수 생성기·문구 표 추출
  jscheck/ simcheck/ realmatch/ gendata/ codecheck/   Go(goja) 확인 도구 5종
test/
  fixture.js       합성 스쿼드
  headless.html    단계 1 확인 페이지
  determinism.html 단계 2 확인 페이지
  tactics.html     단계 3 확인 페이지
```

### 브라우저 없이 확인하는 도구

이 저장소에는 Node 같은 로컬 JS 런타임이 없습니다. 그래서 Go 안에서 JS 를 돌립니다(goja).
데이터 파이프라인에서 쓴 방법을 그대로 씁니다 — ESM 모듈을 의존 순서대로 이어 붙이고
`import`/`export` 줄만 걷어내면 됩니다.

```bash
cd tools/jscheck && go run . ../../src ../../test      # 문법 검사 (.js + <script>)
cd tools/simcheck && go run . -root ../.. -key press   # 전술 슬라이더가 연결됐는지
```

`simcheck` 은 같은 시드로 슬라이더만 바꿔 두 판을 돌리고 결과 지문을 비교합니다.
지문이 같으면 엔진이 그 값을 안 읽는 것입니다 — **패스 길이 미반영을 이걸로 잡았습니다.**

> ⏱ goja 는 V8 보다 6배쯤 느립니다(정규 90분 ≈ 48초). 대량 측정에는 안 맞고,
> "연결됐는가"를 보는 용도입니다. 통계 측정은 브라우저의 `test/tactics.html` 로 합니다.
> 짧게 보려면 `-secs 900` 처럼 경기 길이를 줄이세요.

### 배포 전에 — 버전 도장을 찍는다

```bash
python tools/stamp_version.py
```

`src/**/*.js` 내용으로 짧은 해시를 만들어 **모든 모듈 주소에 `?v=<해시>`** 를 박습니다.
GitHub Pages 는 응답 헤더를 우리가 정할 수 없어서, 주소를 바꾸는 것 말고는 캐시를 밀 방법이
없습니다. 이걸 안 찍으면 새로 배포해도 브라우저가 **옛 모듈을 그대로 씁니다** —
이 프로젝트에서 여러 번 겪었습니다(고친 코드가 반영되지 않아 한참 헤맴).

- HTML 의 `import`, 모듈끼리의 `import`, `new Worker(...)` 전부에 찍습니다.
  모듈 안쪽 import 까지 찍어야 의미가 있습니다 — 입구만 바꾸면 안쪽은 계속 캐시됩니다.
- 해시는 `?v=` 를 걷어낸 내용으로 계산하므로 몇 번 돌려도 같은 값입니다(멱등).
- `--strip` 으로 걷어낼 수 있습니다.

### 커널을 다시 뽑으려면

원본 KM26이 업데이트되면 두 줄만 돌리면 됩니다. 손으로 고칠 곳은 없습니다.

```bash
python tools/extract_engine.py <KM26 index.html 경로> src/engine/kernel.raw.js
python tools/patch_kernel.py src/engine/kernel.raw.js src/engine/kernel.js
```

추출기는 `MatchSim`에서 시작하는 **의존성 폐포**를 계산해 필요한 선언만 원본 순서대로 뽑습니다.
전역 상태·UI는 경계에서 끊고 `stubs.js`가 대신합니다.

### 두 단계로 나눈 이유

커널은 손으로 고치지 않는 게 원칙입니다. 그런데 KM26 원본 버그 중에는 함수 한복판에 있어서
바깥에서 감쌀 수 없는 것이 있습니다. 그것만 패치기가 처리하고, 나머지는 전부 래퍼로 갑니다.

| 어디에 | 무엇을 | 예 |
|---|---|---|
| `tools/patch_kernel.py` | 함수 한복판이라 감쌀 수 없는 **원본 버그 수정** | PK 3건 (아래) |
| `src/engine/rules.js` | 메서드 경계에서 표현되는 **듀얼 고유 규칙** | 누적 파울 · 퇴장 체력 |
| `src/engine/stubs.js` | 엔진이 참조하는 **전역 문맥** | `G` · `famK` · `refBias` |

패치는 "몇 군데에 걸려야 하는지"를 못박아 둡니다. 원본이 갱신돼 코드가 달라지면
조용히 건너뛰지 않고 **즉시 중단**됩니다.

### 고친 KM26 원본 버그

셋 다 페널티킥 관련이고, 듀얼에 그대로 상속되던 것입니다.

| id | 증상 | 원인 |
|---|---|---|
| `PK-01` | PK 다음 킥오프에서 센터서클 한복판에 "페널티킥"이 선언됨 | `kickoff()` 이 `b.isPenalty` 를 초기화하지 않음 |
| `PK-02` | PK 득점이 VAR 로 취소됨 | 2D 엔진에 `isPen` 가드가 없음 (분 단위 엔진에는 있음) |
| `PK-03` | 90분 종료 직전에 선언된 PK 가 실행되지 않음 | 시뮬 루프가 시계만 보고 끝냄 |

> 📮 원본에도 이슈로 올릴 값어치가 있습니다 — 백로그에 있습니다.

---

## 설계 원칙

| 원칙 | 이유 |
|---|---|
| **커널은 절대 손대지 않는다** | 원본이 갱신되면 다시 뽑아야 하므로. 고칠 게 있으면 래퍼(`rules.js`)에서, 그래도 안 되면 패치기에서 |
| **패치는 걸린 횟수를 못박는다** | 원본이 바뀌어 패치가 빗나가면 조용히 넘어가는 대신 즉시 멈추도록 |
| **시즌 누적값은 전부 고정** | 조직력 100 · 사기 75 · 컨디션 100. "누가 시즌을 오래 굴렸는가"가 승부에 끼어들면 안 됨 |
| **포지션 능숙도는 유지** | 이게 없으면 라인업을 짤 이유가 사라짐 |
| **시드는 양쪽 코드에서 유도** | 어느 쪽도 유리한 결과를 골라 공유할 수 없게 |

전체 설계는 `개리그매니저듀얼_설계서_v2.md` 를 보세요.

---

## 개발 단계

| | 단계 | 상태 |
|---|---|---|
| 0 | 준비 · 권한 | ✅ |
| 1 | 엔진 추출 | ✅ |
| 2 | 결정론(시드) 확보 | ✅ **전수 검증 통과** |
| 3 | 전술 반영 검증 · PK 버그 수정 | ✅ 9종 중 7종 뚜렷 (폭·멘탈리티 보류) |
| 4 | 데이터 파이프라인 (Go) | ✅ 1,024명 |
| 6 | 라인업 편집 UI | ✅ 전술판 · 역할 · 저장 슬롯 |
| 7 | 경기 재생 UI | ✅ 해설 재생 · 2D 하이라이트 · 팬 반응 |
| **A** | **연습 모드 (혼자 하기)** | ✅ 3난이도 × 전술 3종 · AI 감독이 후반에 움직인다 |
| 5 | 코드·링크 규격 (대전) | ✅ 300비트 · 51자 · 초대/결과 링크 |
| 8 | 조건부 지시 | ✅ 조건 15종 × 행동 15종 · 여섯 칸 |
| 9 | 밸런스 검증 | ⬜ **다음** |
| 10 | 배포 | ⬜ |

---

## 단계 4 — 데이터 파이프라인

29개 구단 **1,024명**의 실제 선수를 `data/*.json` 으로 뽑아 둡니다.

```bash
python tools/extract_data.py <KM26 index.html 경로> src/data/gen.js
cd tools/gendata
go run . -root ../..
```

### 능력치를 옮겨 적지 않고 원본 함수를 그대로 돌립니다

원본 안에 선수 능력치가 통째로 들어 있지는 않습니다. 들어 있는 건 표 세 개뿐입니다.

| 표 | 내용 |
|---|---|
| `D1` | K리그1 12개 구단 — 손으로 매긴 `ovr` 이 있는 명단 |
| `D2` | K리그2 17개 구단 — 팀 수준(`base`)과 간판 선수 몇 명 |
| `ROSTER26` | 29개 구단 실제 등록 명단 1,024명 (이름·포지션·생년월일·등번호·키·몸무게, **능력치 없음**) |

세부 능력치 40여 종은 `mkPlayer()` 가 만들어 냅니다. 그 안에 포지션 특화 · 나이 감쇠 ·
신장 보정 · 특성 추첨이 겹겹이 들어 있어서, **다른 언어로 옮겨 적으면 반드시 어긋납니다.**
값이 달라지면 "같은 선수"가 아니게 되고 검토 기준 자체가 무너집니다.

그래서 옮겨 적지 않고 **원본 함수를 그대로 뽑아 한 번 돌립니다.** 엔진 커널과 같은 방법입니다.
Go 도구가 그 JS 를 실행(goja)하고, 고르고, 검증하고, 저장합니다.

```
index.html ──extract_data.py──> src/data/gen.js ──go run ./tools/gendata──> data/*.json
             (의존성 폐포 + 시드화)              (실행 · 정리 · 검증 · 해시)
```

### 확인한 것

| 검증 | 결과 |
|---|---|
| 선수 수 | 1,024명 — 원본 명단과 일치 |
| 재현성 | 같은 시드 → 바이트 단위로 같은 파일 |
| 선호 포지션 우선순위 | 안양 마테우스 = **CAM** (`PLAYER_TWEAK` 이 최종 승자) |
| 구단별 정합 | 29개 구단 전부 골키퍼 보유 · 12명 이상 · `attr`/`posFam` 누락 0 |

> 📌 **`ovr` 은 표 값 그대로가 아닙니다.** `applyAgeDecline` 이 나이 감쇠만큼 같이 내립니다.
> (조현우 80 → 78, 35세) 원본 설계가 그렇습니다.

### ⚠ 발견한 원본 버그 — K리그2 간판 선수의 능력치가 무시됩니다

`mkTeam` 이 수기 능력치를 찾을 때 `d.players` 만 봅니다. 그런데 **K리그2 항목은 그 값을 `stars` 에 담고 있어서**,
K2 선수는 전원 추정값(`estimateOvr`)으로 덮입니다.

| 선수 | 표 | 실제 생성 |
|---|---|---|
| 세징야 (대구) | 74 | **63** |
| 양형모 (수원) | 74 | **64** |
| 발디비아 (전남) | 75 | **68** |

밸런스에 직결되므로 **아직 고치지 않았습니다.** 원본에도 이슈로 올릴 값어치가 있습니다.

---

## 단계 6 — 라인업 편집 화면

<https://goormigrm.github.io/KMD26v1.0/lineup.html>

KM26 과 같은 **한 줄 5칸 격자** 전술판입니다. 이 게임을 할 사람들은 이미 개리그 매니저에
익숙하므로 형태를 그대로 씁니다. 격자 뒤에는 **코트를 깔았습니다** — 중앙선·센터서클·
페널티박스·골에어리어. 격자만 있으면 그 자리가 경기장 어디쯤인지 감이 안 옵니다.
칸 크기는 오른쪽 선발 명단 한 줄과 맞췄습니다(이름이 잘리지 않는 크기).

| | 방법 |
|---|---|
| 마우스 | 끌어서 놓기. 전술판 안에서 끌면 두 선수가 맞바뀝니다 |
| 손가락 | 자리와 선수를 차례로 누르기 — 순서는 아무래도 좋습니다 |

### 폰의 탭 줄 — 한 줄로 두고 **옆으로 굴린다**

좁은 화면에서 왼쪽 바는 위쪽 탭으로 눕습니다. 이 판단은 **두 번 뒤집혔습니다.**
그 과정이 판단 근거 자체이므로 남겨 둡니다.

| 언제 | 어떻게 | 왜 |
|---|---|---|
| 탭 3개 | 굴리는 줄 (`overflow-x`) | 한 줄에 들어갔다 |
| 탭 6개 | **접는 줄** (`flex-wrap`) | 뒤쪽 탭(듀얼 기록실)이 늘 숨었고, PC 의 모바일 보기에서는 마우스로 끌어도 안 굴러가 **아예 닿을 수 없었다** |
| 탭 9개 | **다시 굴리는 줄** | 접으면 **세 줄**(102px)이 되어 첫 화면을 덮는다 |

접는 쪽을 버린 것은 그 판단이 틀려서가 아니라 **개수가 달라졌기 때문**입니다.
여섯 개까지는 접는 쪽이 나았습니다. KM26 갈래(라인업·게시판·기록실)가 붙어 아홉이
되면서, 세로 102px 을 길잡이에 내주는 것이 화면을 덮는 문제로 바뀌었습니다.

닿을 수 없던 문제는 그대로 남아 있으므로 **`src/ui/navscroll.js` 를 되살렸습니다.**
마우스 끌기·세로 휠을 옆으로 옮겨 주고, **끌고 난 뒤의 클릭을 삼킵니다**(안 그러면
탭을 밀 때마다 그 페이지로 넘어갑니다). 넘칠 때만 오른쪽 끝을 흐리게 말아
"더 있다"를 알립니다(`#side.hasmore::after`).

> 💡 굴리는 줄이 맞는 자리 — **항목이 늘어나는 목록**. 접는 줄이 맞는 자리 —
> **개수가 고정된 길잡이**. 탭 줄은 그 경계에 있어서, 개수가 넘어가면 판단도 넘어갑니다.

첫 페이지에도 같은 자리로 가는 버튼을 뒀습니다 — 탭을 못 찾는 사람이 있습니다.

### 역습은 켬/끔이 아니라 다섯 단계다

원본 KM26 이 역습을 0~4 슬라이더로 바꿨습니다(`ctrLv` — `index_new.html:3432`).
실제 세이브를 열어 보니 **29팀 중 19팀이 2, 9팀이 3, 1팀이 4** 로, 켬/끔으로는
받을 수 없는 값이었습니다. 그래서 여기도 다섯 단계로 넓혔습니다.

**지킨 것 — 3단계가 옛 '켬'과 완전히 같아야 한다.** 이미 나눠 가진 51자 코드는
역습을 1비트로 담고 있어 해독하면 3 또는 0 이 됩니다. 3단계가 조금이라도 다르면
그 코드들이 **조용히 다른 경기**를 냅니다.

그래서 원본 신판이 함께 바꾼 새 계수(0.16→0.22 등)는 **따라가지 않았습니다.**
여기 있던 값을 그대로 두고 `w = 레벨/3` 으로 재기만 합니다.

| 무엇 | 옛 켬 | 새 식 | 3단계에서 |
|---|---|---|---|
| 역습 창 길이 | 7초 | `7*w` | 7초 |
| 전진 패스 가산 | `forward*0.65` | `forward*0.65*w` | 같음 |
| 스루패스 배수·상한 | `1.8` / `0.62` | `1+0.8w` / `0.45+0.17w` | 1.8 / 0.62 |
| 볼 지체 | `×0.55` | `×(1-0.45w)` | ×0.55 |

부동소수점까지 확인했습니다 — `w=1` 에서 세 식 모두 리터럴과 **비트 단위로 같습니다**.

**실측 확인** (울산 vs 전북, 자동 라인업)

| 역습 | 지문 | |
|---|---|---|
| 0 | `aaa6474d` | 옛 끔과 같음 |
| 3 | `f6b0083c` | **옛 켬과 같음** (같은 시드에서 대조) |
| 1 · 2 · 4 | `72e36c2c` · `95cbc581` · `f758b9a5` | 다섯 칸이 전부 다른 경기 |

> ⚠ **미지정(`undefined`)은 0 이다.** 원본은 미지정을 2(보통)로 보지만 그건 그쪽
> `TAC_DEF.counter` 가 2 이기 때문입니다. 여기는 `false`(끔)라 0 이 맞습니다.
> 처음에 2 로 뒀다가 `planSig` 로 시드를 뽑는 `realmatch` 의 기준 지문이 깨졌습니다
> (전술을 안 넘긴 경기의 서명이 `…,0` → `…,2` 로 바뀜). KM26 세이브에서 값이
> 빠져 있을 때만 **읽는 쪽에서** 2 로 채웁니다.

**데이터 파일은 건드리지 않았습니다.** `data/teams.json` 의 `tacDef.counter` 는
`false` 그대로입니다 — 고치면 데이터 해시가 바뀌어 이미 나눠 가진 코드가 전부
막힙니다. 화면·코덱·커널이 각자 `ctrLevel`/`ctrLv` 로 옮겨 읽습니다.

### 첫 페이지 버튼은 글이 짧아야 한다

`.go` 는 `display:flex` 라 **제목과 부제가 옆으로 붙습니다.** 첫 페이지의 곁버튼처럼
칸이 좁으면 부제가 두 줄로 접혀 버튼마다 높이가 들쭉날쭉해집니다.

- 곁버튼은 `flex-direction:column` 으로 **세로로 쌓습니다.**
- 글은 짧게 — 부제가 **한 줄에 들어가는 길이**로 씁니다(폰에서 7~9자).
  실측: 375px 에서 곁버튼 넉 장이 모두 57px 로 같아졌습니다.
- 곁버튼은 폰에서도 **두 칸을 유지**합니다. 한 칸으로 떨어뜨리면 첫 화면이 버튼
  다섯 개로 길어져 아래 설명이 화면 밖으로 밀립니다.

### 화면 구성 — 왼쪽 바

KM26 과 같은 구조입니다. 넓은 화면에서는 **왼쪽 210px 고정 바**, 좁은 화면에서는
**위쪽 탭**으로 눕고 **한 줄로 두되 넘치면 옆으로 굴립니다**
(아래 "폰의 탭 줄" 절 참고). 어느 화면에 있든 다른 화면으로 한 번에 갑니다.

| 색 | 뜻 |
|---|---|
| 🟩 초록 큰 버튼 | **다음으로 간다** — 경기 시작 · 이 라인업으로 경기 (KM26 `.bigbtn` 과 같은 색) |
| 🟨 금색 큰 버튼 | **대전 코드 발급** — 사람에게 건네는 것. 초록과 헷갈리지 않게 색을 갈랐습니다 |

### 대전 코드는 눈에 보이는 자리에 있습니다

라인업 화면에서 **🔗 대전 코드 발급**을 누르면 코드와 초대 링크가 나옵니다(기본은 빈칸입니다 —
라인업이 정해지기 전에 코드를 보여 주면 헷갈립니다). 경기 화면으로 넘어가면 대진 칸 아래에
**내 대전 코드**가 금색 상자로 떠 있어 바로 복사할 수 있습니다.

상대 코드는 **붙여넣는 칸**에 넣습니다 — 클립보드를 읽지 않습니다.
`🔗 대전 코드 붙여넣기` 를 고르면 두 줄짜리 입력칸이 열립니다.

### 상대는 펼쳐 놓고 고릅니다

드롭다운을 열어 두는 대신 **칩을 펼칩니다.** 홈·원정 각각 리그 탭(K리그1/K리그2) →
구단 칩 → 라인업(자동 · 라인업 화면에서 짠 것 · 저장 슬롯 · 🔗 대전 코드 · 🤖 연습 3난이도).
값의 주인은 그대로 숨은 `<select>` 라, 화면 로직은 손대지 않았습니다.

### 저장 슬롯이 맨 위입니다

짜 둔 라인업이 있으면 **구단을 고르기 전에 그것부터 불러오는 게 순서**입니다. 불러오면
구단·포메이션·전술·배치가 한꺼번에 올라옵니다. 그래서 슬롯 카드를 구단 선택보다 위에 둡니다.

묻는 기준은 **"저장해 둔 것이 사라지는가"** 하나입니다.

| 무엇 | 묻나 | 왜 |
|---|---|---|
| 카드 누르기 = 불러오기 | **안 묻는다** | 슬롯을 건드리지 않는다. 무엇이 올라오는지 카드에 다 적혀 있다 |
| 빈 칸에 저장 | 안 묻는다 | 잃을 것이 없다 |
| `💾` 덮어쓰기 · `✕` 지우기 | **묻는다** | 저장해 둔 한 벌이 되돌릴 수 없이 사라진다 |

처음에는 셋 다 물었습니다. 그런데 확인 상자(`#slotAsk`)가 슬롯 격자 **아래**에 붙어 있어서,
폰에서는 카드를 누른 자리와 상자가 화면 한 칸씩 떨어져 보였습니다 — "눌렀는데 아무 일도
안 일어나고 엉뚱한 게 떴다"로 읽혔다는 제보를 받았습니다.

불러오기는 저장분을 건드리지 않으므로 **그 자리에서 바로 합니다.** 대신 무엇이 올라왔는지
`flash(…, ok)` 로 한 줄 알리고(성공은 `⚠` 가 아니라 `✔`), `#main` 으로 스크롤해 줍니다 —
폰에서는 슬롯이 맨 위, 전술판이 한참 아래라 아무 표시가 없으면 여전히 "안 눌렸다"로 보입니다.

> ⚠ `flash()` 는 **상태만 바꿉니다.** 화면에 올리려면 `drawHint()` 를 이어서 불러야 합니다
> (`draw()` 가 곧바로 덮어쓰기 때문에 그렇게 만들어 뒀습니다).
> 이걸 빠뜨려 불러오기 알림이 안 뜬 적이 있습니다.

### 선수 프로필 — 페이지를 옮기지 않습니다

선수 옆 **프로필** 버튼을 누르면 그 자리에서 세부 능력치가 열립니다 —
기술·정신·신체·골키퍼 능력치, 포지션 능숙도 17칸, 특성, 그리고 **지금 서 있는 자리의 능숙도**.
선수단 화면으로 넘어가면 짜던 라인업이 날아가기 때문입니다. 이름표는 `data/teams.json` 의
`labels` 를 씁니다 — 화면에 베껴 두면 원본이 바뀔 때 어긋납니다.

> ⚠ **전술판 칸(`#pgrid .cell`)에는 프로필 버튼을 두지 않습니다.** 칸이 64px
> (폰에서는 56px)뿐이라, 선수를 바꾸려고 누른 손가락이 버튼에 걸려 능력치 창이 열리는
> 일이 잦았습니다(제보). **칸은 자리를 고르는 일 하나만** 합니다 — 한 요소에 두 가지
> 뜻을 주면 좁은 화면에서 반드시 부딪힙니다.
> 프로필은 줄이 넓은 **선발 명단 · 선수 목록 · 교체 명단**에만 둡니다.

명단은 KM26 처럼 **선발 / 교체 명단(S1~S9) / 그 외 명단** 셋으로 나뉩니다.
교체 명단은 목록이 아니라 **자리가 아홉 개**라 순서가 눈에 보입니다.

**자리에 안 맞는 선수도 세울 수 있습니다.** 걸러내지 않고 능숙도 색으로만 알립니다 —
지고 있을 때 센터백을 최전방에 올리는 건 실제로 하는 일이니까요.

역할·임무도 고를 수 있습니다. 엔진이 `tactic.role[선수id]` 를 읽으므로 경기에 그대로 반영됩니다.
그 자리에서 맡을 수 없는 역할이면 자리 기본값으로 되돌아갑니다 — 커널 `getRole` 과 같은 판정입니다.

### 별점은 눈금이 하나입니다

KM26 은 1군 수준에 못 미치는 선수를 **은색 눈금**으로 갈아태웁니다. 시즌 게임에서는
유스와 1군을 갈라 보여 주는 장치라 뜻이 있지만, 듀얼에서는 해롭습니다 —
금색 2.5개짜리 주전보다 은색 3.5개짜리 후보가 **별이 많아 보입니다.** 실제로는 한참 아래인데도요.

듀얼은 육성도 잠재력도 없고 오로지 지금 실력으로 겨루므로 눈금을 하나로 합쳤습니다.
K리그1·2 전체 선수의 실력 분포에서 **하위 2%가 0.5★, 상위 2%가 5★** 입니다.
별이 많을수록 언제나 더 나은 선수입니다.

### 구단 별점은 눈금이 따로입니다 (`src/ui/teamstar.js`)

선수 눈금을 열한 명 평균에 그대로 쓰면 값이 위로 몰려 **K리그1 이 전부 4.5~5★ 로** 붙습니다.
우승 후보와 중위권이 같은 별로 보이니 쓸모가 없습니다. 그래서 구단은 구단끼리 비교하는
별도 눈금을 씁니다 — KM26 과 같은 구간(주전 평균 능력치 **60 = 0.5★, 77.5 = 5★**)이고,
실제 29개 구단은 63.5~75.3 에 깔려 1.5★~4.5★ 로 퍼집니다.

**선수단 · 라인업 · 경기** 세 화면이 이 별을 보여 줍니다. 같은 구단인데 화면마다 다른 별이
뜨면 안 되므로 계산은 `src/ui/teamstar.js` **하나**에 있습니다 — 전에는 선수단과 라인업이
같은 코드를 각자 들고 있었습니다. 이건 **엔진 모듈이 아니므로** `realmatch`·`simcheck` 의
목록에 넣지 마세요(`src/board/board.js` 와 같은 부류입니다).

> ⚠ **별 다섯 개는 생각보다 넓습니다.** 14px·자간 2px 이면 80px 이고 숫자까지 100px 입니다.
> 선수단의 구단 카드 속폭은 85px 이라 **15px 이 넘쳐** 숫자가 테두리 밖으로 나가 있었습니다.
> 카드 안에서는 `.team .stars` 로 11px·자간 0.5px 까지 줄이고, 넘치면 잘라 냅니다.
> 자간은 **마지막 글자 뒤에도** 붙으므로 음수 여백으로 되돌립니다.

---

## 단계 7 — 경기 재생 화면

<https://goormigrm.github.io/KMD26v1.0/match.html>

라인업 화면의 **▶ 이 라인업으로 경기** 를 누르면 선발·교체·전술·역할이 그대로 엔진에 들어갑니다.
대진 칸에서는 **양쪽 모두** 라인업을 고를 수 있습니다 — 자동 · 라인업 화면에서 짠 것 ·
저장 슬롯 열 벌. 그 구단으로 저장한 것만 목록에 나옵니다.

### 같은 구단끼리도 붙습니다

**울산 vs 울산** 이 됩니다. 선수와 전술이 다르면 다른 팀이니까요.
막는 것은 단 하나 — **라인업 지문이 완전히 같을 때**입니다(선발·교체·전술·역할이 한 글자도
다르지 않은 경우). 그때는 두 팀을 가릴 것이 없습니다.

같은 구단이면 두 가지를 갈라 놓습니다.

| 무엇 | 어떻게 | 왜 |
|---|---|---|
| 선수 id | 원정 쪽에 **+100000** | 커널은 22명을 한 배열에 담고 id 로 찾습니다(`byId`). 겹치면 원정 슈터를 찾을 때 홈 선수가 잡힙니다 |
| 이름·색 | `(홈)` · `(어웨이)` · 원정은 `col2` | 해설·팬 반응·2D 에서 어느 쪽인지 알아야 합니다. 게다가 커널 `ev()` 는 "직전 줄과 같은 팀인가"를 **이름표로** 판단해 시간 배지를 생략합니다 |

> ⚠️ 옮긴 id 는 **엔진 안에서만** 삽니다. 시드(= 단계 5 의 대전 코드)는 반드시 옮기기 **전**
> 라인업으로 뽑아야 두 사람의 코드가 같은 값을 냅니다.

### 전술판의 모양과 자리가 엔진까지 갑니다

라인업 화면이 넘기던 값에 **포메이션이 빠져 있었고**(경기는 늘 4-3-3 으로 섰습니다),
전술판에서 끌어다 놓은 **자리 표도 비워서 넘겼습니다**(커널이 전부 자동 재배치했습니다).
둘 다 이제 그대로 갑니다 — `tactic.formation` 과 `tactic.slot` 입니다.
커널 `computeRenderSlots()` 는 감독이 지정한 자리를 최우선으로 지키고, 남은 선수만
그 포메이션의 빈 자리에 앉힙니다. 옛 저장본에 포메이션이 없으면 자리 이름 묶음으로 되찾습니다.

**실시간으로 한 틱씩 돌려 보여 주지 않습니다.** 먼저 끝까지 다 돌린 뒤 이벤트를 분 단위로
풀어놓는 **재생** 방식입니다. 실시간이면 엔진 속도가 곧 배속 상한이 되지만, 다 돌려 놓고 재생하면
1·2·4배속도 끝까지 건너뛰기도 공짜입니다.

전광판은 이벤트에 박혀 있는 **그 순간의 점수**를 따릅니다 — 원본이 그러라고 넣어 둔 값이고,
안 그러면 점수가 골 해설보다 먼저 올라갑니다.

```
src/engine/teams.js        data/*.json → 엔진 팀 객체 · 자동 라인업 · 라인업 검사
src/engine/matchworker.js  경기를 따로 떼어 돌린다 (한 판 8초라 화면이 멈친다)
match.html                 전광판 · 해설 재생 · 배속 · 경기 기록
```

### 2D 하이라이트

결정적 장면이 나오면 **시계를 멈추고 그 장면을 캔버스로 보여 준 뒤** 이어서 재생합니다.
경기가 끝나면 목록에서 다시 볼 수 있습니다.

**필드 위에는 아무것도 얹지 않습니다.** 한동안 왼쪽 위 이름표(`12' 선방`) · 아래 자막 ·
오른쪽 위 버튼 두 개를 캔버스에 덮어 두었는데, 둘 다 실패였습니다.

1. **결과가 샜습니다.** 이름표가 무슨 장면인지를 공이 닿기도 전에 말해 버립니다.
   골 해설을 붙들어 두는 장치(`held`)까지 만들어 놓고 배지로 `12' 골` 이라고 쓰면
   붙들어 둔 보람이 없습니다. `quiet` 옵션으로 배지만 감춰 보기도 했지만,
   **감출 것이면 애초에 띄우지 않는 것이 맞습니다.**
2. **필드보다 글자가 먼저 보인다**는 제보를 받았습니다. 캔버스는 화면 폭에 맞춰 줄어드는데
   덮개는 고정 크기라, 폰에서는 필드의 상당 부분을 글자가 가렸습니다.

무슨 일이 벌어졌는지는 **2D 바로 아래 한 줄**(`.tick`)이 제때 말해 줍니다 — 붙들어 둔
골 해설도 `push()` 를 지나므로 자동으로 제때 올라옵니다. 장면 조작(`↺ 다시 보기` ·
`⏩ 장면 건너뛰기`)은 아래 버튼 줄로 내렸고, 볼 것이 있을 때만 나타납니다.
남은 것은 **득점자 패널**뿐인데, 그건 공이 골망에 꽂힌 **뒤에** 뜨므로 미리 알려 주지 않습니다.

> 관전/장면을 가르는 표시는 **밝기 하나로** 남았습니다(`.hl.idle` 의 `filter`).
> 글자 없이도 "지금은 눈여겨볼 대목이 아니다"가 전해집니다 — KM26 `#pitch2d.dim` 과 같은 자리입니다.

**딱 하나 되살린 덮개 — `● 리플레이`.** 골 장면은 끝나고 한 번 더 느리게 돌아가는데,
아무 표시가 없으니 **같은 화면이 되풀이되는 고장**으로 읽혔습니다. 오른쪽 위에 빨간
글씨로 답니다(방송의 REPLAY 표시와 같은 뜻). 이것만 예외인 이유는 **결과를 미리 알려
주지 않기 때문**입니다 — 이미 본 장면을 다시 보는 중이라는 사실만 말합니다.

### 해설이 두 갈래라는 것 (`caps`)

커널은 해설을 **두 군데**로 냅니다. 듀얼은 오랫동안 한쪽만 썼습니다.

| 갈래 | 어디로 | 90분에 | 원래 쓰임 |
|---|---|---|---|
| `say()` | `M.events` | 70~90줄 | 문자중계 로그 — 시즌 기록에 남는다 |
| `cap()` | `sim.caps` | **600줄쯤** | 방송 자막 — 링버퍼(90줄)라 지나가면 버려진다 |

2D 는 0.2초마다 움직이는데 글자는 한 분에 한 번 바뀌니 멈춘 것처럼 보였습니다.
원인은 **촘촘한 쪽을 통째로 버리고 있었던 것**입니다. `replay.js` 가 `cap()` 을 감싸
링버퍼에서 밀려나기 전에 따로 쌓습니다(`takeCaps`).

- ⚠ **난수를 쓰지 않습니다.** 원래 `cap()` 을 먼저 부르고 결과만 읽습니다
  (문장을 고르는 `F_` 는 원래 호출 안에서 이미 돌았습니다). 지문 `aaa6474d` 그대로입니다.
- ⚠ **분이 아니라 초(`sec`)로 남깁니다.** 클립 프레임에도 같은 시계(`c`)가 박혀 있어,
  화면이 지금 그리는 시각에 자막을 맞출 수 있습니다.
- ⚠ 자막에는 `골`·`선방` 처럼 **결과를 말하는 줄이 섞여 있습니다.** 그래서 붙들어 둔
  장면이 있으면(`held`) 자막도 함께 붙듭니다 — 안 그러면 붙들어 둔 보람이 없습니다.

#### ⚠ 자막은 아무 데서나 흘리면 안 된다 — 배속이 다르다

처음 붙였을 때 **"2D 화면과 자막이 안 맞는다"** 는 제보를 받았습니다.
자막 데이터는 멀쩡했습니다 — 실측하면 골 자막이 경기 시계 **1626초**, 그 골이 들어가는
클립 프레임이 **1627초**로 정확히 붙어 있습니다. 어긋난 것은 **필드가 흐르는 속도**였습니다.

| 어디 | 필드 배속 | 자막 한 줄이 화면에 머무는 시간 |
|---|---|---|
| 장면 클립 (0.4초를 50ms 에) | **8배** | 1.1초 — 읽을 만하다 |
| 초반 3분 (1분을 7,500ms 에) | **8배** | 1.1초 |
| 그 밖의 관전 (1분을 800ms 에) | **75배** | **0.12초 — 읽을 수 없다** |

자막은 KM26 의 **하이라이트 재생(8배속)** 을 전제로 쓰인 문장들입니다. 듀얼은 관전 구간을
75배속으로 흘리므로, 거기서는 한 줄을 읽는 1초 사이에 화면이 **경기 시계로 70초**를
지나갑니다. 글자와 공이 따로 노는 것이 당연합니다.

**그래서 자막은 필드가 8배속인 구간에서만 흘립니다** — 장면 클립과 초반 3분.
나머지 관전 구간은 문자중계 줄만 씁니다(분 단위 화면에는 분 단위 글이 맞습니다).

화면(`flowCaps`)에서 지키는 것.

1. **화면이 그리는 시각까지만** 흘린다 — 관전 중이면 `watchSec`, 장면 중이면 `frame.c`.
   시각에 묶여 있으므로 공보다 글자가 앞설 수 없습니다.
2. **한 줄에 최소 머무는 시간**(`CAP_DWELL`)을 둔다. 그 사이 지나간 줄은 버리고
   **가장 새 줄만** 보여 줍니다. 문자중계 줄은 더 오래 붙잡습니다(`EVENT_HOLD`).
3. **관전 중에는** 붙들어 둔 장면(`held`)이 있으면 자막도 막는다 — 관전 헤드는 해설보다
   최대 1분 앞설 수 있습니다. **장면 재생 중에는 막지 않습니다**: 프레임 시각에 묶여 있는
   것 자체가 이미 안전장치입니다.

장면 클립은 **이미 지나간 시각**을 다시 보여 주므로(앞 22초부터) 자막 커서도 함께
되감습니다(`rewindCaps`). 여기가 자막이 가장 잘 맞는 자리이자, "2D 는 움직이는데 글자는
멈춰 있다"가 가장 크게 느껴지던 자리입니다.

### 해설은 2D 보다 뒤에 나옵니다 (`held`)

`tick()` 은 그 분의 해설을 먼저 다 풀고 나서 장면을 틉니다. 그대로 두면 **결과를 읽고 나서
그 장면을 보게 됩니다** — `크로스바를 강타합니다!` 를 읽은 뒤에 그 슛을 보는 것은 거꾸로입니다.

그래서 **그 줄이 말하는 장면이 곧 재생될 것이면 줄을 붙들어 둡니다**(`held`).
공이 실제로 그 일을 하는 프레임(`clip.trig` — 커널이 `markHighlight` 를 부른 지점)에서
한꺼번에 풉니다. KM26 의 `SCENE_CAPTION_AT` 과 같은 생각인데, 거기서는 골만 그렇게 합니다.
여기서는 **장면이 잡힌 모든 대목**(골·PK·퇴장·선방·결정적 기회)에 겁니다.

| 함정 | 왜 |
|---|---|
| **한 줄만 붙들면 안 된다** | 전광판이 `e.hg` 를 따라가므로 뒷줄 하나만 먼저 나가도 점수가 올라간다. 그리고 골 다음 줄이 "실점에 격분한…" 이라 문장 자체가 결과를 말한다 |
| 붙든 장면과 **실제로 트는 장면이 다를 수 있다** | `holdingClip` 은 무게 큰 순, `clipAt` 은 이른 분 순으로 고른다. 트기 직전에 `held.c = c` 로 갈아 끼운다 — 안 그러면 한 분 뒤에 뒤늦게 풀린다 |
| 장면이 **끝내 안 나올 수 있다** | 장면 보기를 껐거나 무게 문턱이 높으면 그렇다. `minute > held.c.min + 1` 안전망으로 푼다. 건너뛰기·끝까지·경기 종료에서도 `releaseHold(null)` 로 반드시 푼다 |

### "재생 중"과 "시계"는 다른 것입니다

한동안 `pause()`/`resume()` 하나로 둘을 묶어 두었습니다. 그래서 장면을 트느라 시계를 멈출
때마다 버튼이 `▶ 재생` 으로 바뀌었고 — **장면이 돌아가는 중인데 버튼은 "멈춰 있다"고
말했습니다.** 거기서 재생을 눌러도 이미 돌아가고 있으니 아무 일도 안 일어나 보였고,
"버튼이 안 먹는다"는 제보로 돌아왔습니다.

| 이름 | 무엇 |
|---|---|
| `playing` | 사용자가 "돌아가라"고 해 둔 상태. **장면을 보는 동안에도 참**이다 |
| `startClock`/`stopClock` | 분을 세는 타이머. 장면을 보는 동안에는 멈춘다. 버튼 표시는 건드리지 않는다 |
| `pause()`/`resume()` | 사용자가 누른 것. `pause()` 는 **`clipTimer` 도 함께 멈춘다** — 화면이 실제로 서야 눌린 것으로 보인다 |
| `over` | 경기가 끝났다. 시계를 다시 걸지 않는다(다시 보기는 그대로 된다). 재생·끝까지 버튼은 끈다 |

### 폰에서 경기 화면은 **필드에 자리를 몰아 준다**

글자 크기만 줄여서는 부족했습니다. 375×640 으로 재 보니 **필드 위가 227px** 을
차지하고 있었습니다.

| 무엇 | 전 | 후 | 어떻게 |
|---|---|---|---|
| 위쪽 탭 | 117px (늘 붙어 있음) | 0 | 경기 중에는 `position:static` — 내리면 비켜 준다 |
| 제목 `.top` | 44px | 0 | 경기 중에는 감춘다. 알려 줄 것이 없다 |
| 전광판 | 66px | 47px | 점수와 시계를 **한 줄로**(`.mid` 를 flex 로) |
| 버튼 줄 | 100px (5줄) | 61px (3줄) | 긴 말을 접는다(`.lg`), 이름표("속도"·"장면")를 감춘다 |
| 해설 로그 | 230px | 200px | |

결과: 필드가 화면 맨 위 66px 에서 시작해, **전광판·필드·자막·버튼이 한 화면에** 들어옵니다.

> ⚠ **경기가 시작되면 화면을 필드로 옮깁니다**(`scrollToField`). 안 그러면 폰에서는
> 대진 화면 자리에 그대로 있어 **아래쪽 빈 기록창만** 보게 됩니다.
> 기준은 필드(`#cv`)가 아니라 **전광판(`.board`)** 입니다 — 필드를 맨 위에 맞추면
> 점수가 화면 위로 잘려 나갑니다.
> `smooth` 는 쓰지 않습니다. 시작 직후 8초짜리 계산이 끼어들어 스크롤이 끊깁니다.

> ⚠ **`▶ 경기 시작` 은 폰에서 아래에 붙여 둡니다.** 구단 칩 스물아홉 장을 다 지나
> **1,137px** 을 내려가야 닿는 자리였습니다 — 가장 중요한 버튼이 가장 멀었습니다.
> `position:sticky; bottom:8px` 이고, 대진 화면(`body:not(.ingame)`)에서만 붙입니다.
> 붙은 버튼이 마지막 줄을 영영 가리지 않도록 `#setup` 바닥을 74px 띄웁니다.

### 폰에서는 필드를 기준으로 나머지를 줄입니다

2D 필드는 캔버스가 `width:100%` 라 화면 폭에 맞춰 **함께 줄어드는데**, 글씨·버튼·전광판은
데스크톱 크기 그대로였습니다. 375px 폭에서 필드는 331px 로 줄어드는데 점수는 30px,
팀 이름은 16px 그대로라 **필드보다 주변이 더 커 보였습니다.**

`match.html`·`board.html` 맨 아래 `@media (max-width:760px)` 한 블록에서 함께 줄입니다.
**맨 아래여야 합니다** — 위쪽 `(max-width:820px)`(왼쪽 바)와 먼저 나온 `(max-width:760px)`
규칙을 덮어써야 하기 때문입니다. **캔버스는 건드리지 않습니다.** 기준이 되는 쪽입니다.

**필드는 항상 서 있습니다.** 예전에는 골이 없으면 캔버스를 아예 띄우지 않아
0:0 경기에서는 필드가 한 번도 안 그려졌습니다. 이제 킥오프 포메이션을 세워 두고,
장면이 끝나면 그 **마지막 프레임에서 정지**한 채로 시간이 흘러갑니다
(KM26 의 `MATCH_MODE.TEXT` — "정지된 포메이션 위에 자막만" 과 같은 자리입니다).

| 무엇 | 어떻게 |
|---|---|
| 어디까지 볼까 | `결정적 장면 전부`(기본) · `골만` · `안 보기`. 엔진이 매긴 무게로 가릅니다 — 결정적 기회 1 · 선방 2 · 퇴장 3 · PK 4 · 골 5 (커널 `HL_W`) |
| 골 리플레이 | 골 장면이 끝나면 **골 순간 앞 6초·뒤 2.5초를 0.62배속으로 한 번 더**. KM26 의 `HL_REPLAY_PRE/POST/RATE` 를 그대로 옮겼습니다. 되풀이는 하지 않습니다 |
| ↺ 다시 보기 | 방금 본 장면을 손으로 다시. 정지 화면에서도 누를 수 있습니다 |
| 배속 | **기본 1배속**입니다. 장면 재생에도 걸립니다(KM26 `HL_RATE × liveMul()`) — 한 경기에 장면이 스무 개면 전체 시간이 장면에 지배되므로, 해설 간격만 바꾸면 배속 차이가 느껴지지 않습니다. 보고 있던 장면도 그 자리에서 새 배속으로 이어집니다 |
| 기본 속도 | 1분에 **800ms** · 장면 한 장에 50ms. 예전 값(640ms·40ms)은 눈으로 따라가기에 빨라서 **0.8배로 늦췄습니다.** 단 **처음 3분은 1분에 7,500ms**(=엔진 150장 × 50ms)로, 장면 재생과 같은 속도입니다. 시계만 따지면 90분이 1분 32초쯤 걸립니다 |
| 초반을 늦추는 이유 | 결정적 장면이 없는 구간이라 관전 화면이 37배쯤 빨리 흘렀고, 해설도 몇 줄 없어 "시작했는데 화면이 죽어 있다"로 보였습니다. `OPEN_MINUTES`(match.html)와 `WATCH_FULL_UNTIL`(replay.js)이 **같은 구간을 가리켜야** 합니다 — 화면만 늦추고 좌표가 띄엄띄엄이면 초반이 뚝뚝 끊깁니다 |

### 득점자 패널

골이 들어가는 순간 화면 가운데에 **GOAL! · 득점자 · 도움**을 크게 띄웁니다(KM26 `goalPanel`).
빌드업이 재생되는 동안에는 띄우지 않습니다 — 결과를 미리 알려 주는 꼴이 되기 때문입니다.
클립에 담아 둔 `trig`(장면이 잡힌 지점)에 닿는 순간부터 뜨고, 리플레이 중에는 걷습니다.

> ⚠️ 커널은 **오프사이드·VAR 로 취소된 골에도** `markHighlight("goal")` 을 붙입니다.
> 득점 기록(`goalLine`)에 없으면 취소된 것으로 보고 **NO GOAL** 패널을 띄우고,
> 다시 보기 목록에도 `득점 취소` 로 적습니다. 예전에는 이것까지 "골"로 세어
> 한 골도 안 난 팀에게 골 장면이 두 개 있는 것처럼 보였습니다.

도움 이름은 커널이 `M.sc` 에 담지 않고 해설 문장 안에만 넣습니다. 문장을 되짚어 이름을
뽑아내는 건 깨지기 쉬우므로, `recordGoal` 을 감싸 `goalTag.aid` 에서 받아 적습니다
(`src/engine/replay.js`). 커널은 손대지 않습니다.

### 해설은 아래에서 위로 쌓입니다

가장 새 줄이 **맨 위**, 필드 화면 바로 아래에 옵니다. 짧을 때는 바닥에 붙어 있습니다.

한 경기에 장면이 19~24개 나옵니다. 골만 보던 것에 비하면 필드에서 벌어지는 일이
훨씬 많이 보입니다 — 선방·결정적 기회·퇴장도 경기의 일부입니다.

필드 그림과 바둑알 크기는 KM26 `drawPitchBase` 에 맞췄습니다 —
골 에어리어·골대·페널티 스폿까지 있어야 "골문 앞"이 어디인지 눈에 들어옵니다.

### 좌표를 두 벌로 나눠 보냅니다 — 관전 트랙 · 장면 클립

엔진은 0.2초마다 22명의 좌표를 남깁니다. 90분이면 13,500장, 선수 좌표만 60만 개라
그대로 넘길 수 없습니다. 그렇다고 결정적 장면만 잘라 두면 **그 사이가 정지 화면**이 됩니다 —
90분 중 결정적 장면은 3~4분어치뿐이라, 실제로는 멈춰 있는 시간이 훨씬 길었습니다.

KM26 은 하이라이트가 없는 동안 시뮬을 계속 돌리며 캔버스를 어둡게 덮어 "빨리 감는 중"을
보여 줍니다(`drawSimWatch` + `#pitch2d.dim`). 듀얼은 경기를 **먼저 다 돌려 놓으므로**
그 방식을 그대로 쓸 수 없습니다. 대신 좌표를 두 벌로 나눠 보냅니다.

| 트랙 | 촘촘함 | 무게 | 쓰임 |
|---|---|---|---|
| **관전**(`watch`) | 1.6초에 한 장 | ≈490KB | 장면 사이 — 어둡게, 경기 내내 끊기지 않는다 |
| **장면**(`clips`) | 0.2초에 한 장 | ≈640KB | 골·PK·선방·퇴장 — 밝게, 시계를 멈추고 본다 |

관전 트랙은 평평한 `Float32Array` 한 덩어리라 일꾼에서 화면으로 **복사 없이** 넘어갑니다
(`postMessage(msg, [buffer])`). 화면은 경기 시계에 맞춰 두 장 사이를 보간하므로
1.6초에 한 장이어도 끊겨 보이지 않습니다.

> ⚠️ 보간은 **자리 번호가 아니라 선수 id 로** 짝을 맞춥니다. 교체·퇴장으로 명단이 밀리면
> 자리 번호로는 두 선수가 서로에게 미끄러져 들어갑니다.

> ⚠️ 숨은 탭에서는 `requestAnimationFrame` 이 **아예 멈춥니다**(0프레임). 그래서 분 경계에
> 안전망을 뒀습니다 — 400ms 넘게 갱신이 없으면 거기서 한 장 그립니다. KM26 도 같은 자리에
> 같은 안전망을 두고 있습니다(`stepLiveSim`).

장면 클립은 원본 규격을 그대로 옮겼습니다 — `markHighlight()` 가 짚어 둔 지점의
**앞 11초**(`HL_LEAD`)부터, **공이 잠잠해질 때까지**(`HL_SETTLE` 2.2초, 최소 2.6초·최대 10초),
골이면 **세리머니 9초**(`HL_CELEB`)까지. 뒤를 5초로 고정했던 예전에는 슛이 튀어나온 다음이
잘려 무슨 일이 있었는지 안 보였습니다.

> ⚠️ **`sim.recording` 은 절대 끄면 안 됩니다.** 커널에 `if(this.recording && RNG()<…)` 가 있어
> 끄는 순간 난수 흐름이 달라져 **다른 경기**가 됩니다. 2D 를 보든 안 보든 같은 경기여야 하므로,
> 켜고 끄는 것은 클립 수집(`_wantClips`)뿐입니다.

### 경기 뒤 반응 — 소셜미디어 · FM코리아

경기가 끝나면 팬들이 떠듭니다. 문구·계정 이름·더비 표를 KM26 에서 그대로 가져왔습니다
(`data/reactions.json`). **양쪽 팬 시선을 바꿔** 볼 수 있고, 같은 경기면 반응도 같아야 하므로
경기가 끝난 뒤 시드를 다시 심고 뽑습니다.

원본을 그대로 따라간 것:

| 무엇 | 어떻게 |
|---|---|
| 우리 팬 · 남의 팬 | 표가 **아예 다릅니다**(`SOC/FMK` ↔ `RIV/FRIV`). 같은 경기를 남의 눈으로 본 글은 결이 다릅니다. 한 피드에 섞여 올라오고, 타팬 글에는 이름표가 붙습니다 |
| 계정 이름 | 이모지가 붙고 일부 풀은 뒤에 숫자가 붙습니다(`원정석구석4821 🧳`). 여섯 개 풀 131개 — 뽑는 확률까지 원본 `socHandle()`/`rivalHandle()` 그대로입니다 |
| 펨코 글 | 추천·비추·조회수가 붙습니다. 나쁜 소식에는 **상주 악플러**(시모·독고)가 세금리그 타령을 들고 옵니다 — 비추가 쏟아지지만 개의치 않습니다 |
| 더비 | `RIVALS` 표 31쌍. 슈퍼매치·동해안 더비처럼 **이름이 붙은 대결**은 반응의 양과 결이 다릅니다(대형 더비는 한 줄 더 시끄럽습니다) |
| 상황 | 승/무/패 · 3골 차 이상 · 역전승/역전패 · 무득점(슛을 많이 하고도 vs 아예 못 만든) · 무실점 |
| **안양팬** | 원본에 없는 KMD26 쪽 추가입니다. 안양이 나오는 경기에는 무슨 일이 있어도 안양을 편들고, 안양이 없는 경기에는 무슨 일이 있어도 심드렁한 소리를 합니다. 펨코에만, 한 경기에 한 번 (상한에 걸려 잘리지 않게 따로 챙깁니다). **글은 다른 사람 글과 똑같이 보입니다** — 눈에 띄게 칠하지 않습니다 |

> ⚠️ 예전에는 이 표의 **10종만** 쓰고 있었고, 그중 `bigWin`/`bigLose` 는 소셜에 없는 키라
> 대체 문구로 빠지고 있었습니다(실제 이름은 `winBig`/`loseBig`). 게다가
> `goalHigh/goalOk/goalLow` 는 **시즌 목표 공약**에 대한 반응인데 이것을 "골이 많이 났다"로
> 써서, 5골 경기에 *"감독이 우승 얘기했다는데... 진심인가?"* 같은 줄이 올라왔습니다.
> 지금은 쓰지 않습니다.

시즌이 없으므로 연승·순위·개막전·이적·구장 묶음은 쓰지 않습니다. 관중 반응(`crowd*`)과
수원 이스터에그는 추출기가 아직 안 가져옵니다 — 듀얼은 중립 경기라 관중이 없습니다.

### 부상·퇴장·교체 — 눈으로 확인하는 법

"부상인데 교체가 안 되고 열 명으로 뛰더라"는 제보를 **추측이 아니라 측정으로** 확인하려고
`realmatch` 에 감사 모드를 붙였습니다.

```bash
cd tools/realmatch && go run . -root ../.. -audit 26
```

두 가지를 나란히 찍습니다.

- **`subLog`** — 🚑(부상) · 🟥(퇴장) · 🔁(교체) 줄만 시각순으로
- **`onPitch`** — 관전 트랙에서 **실제로 필드에 서 있는 사람 수**(홈v원정). 숫자가 바뀌는
  순간만 남기므로 열 명으로 뛴 구간이 그대로 드러납니다

> ⚠ **낱말이 아니라 이모지로 거르세요.** 처음에는 `퇴장` 이라는 낱말로 걸렀는데,
> `🟥 … 그라운드를 떠납니다!` 에는 그 낱말이 없습니다. 사람이 사라졌는데 아무 줄도 안
> 남아 "조용히 없어졌다"로 읽혔습니다 — 실제로는 레드카드였습니다.

**26경기 실측 (2026-08-14)**

| 무엇 | 결과 |
|---|---|
| 교체 | 146건 — **양 팀 모두** 정상 작동 (듀얼은 둘 다 `isUser=false` 라 AI 감독이 맡습니다) |
| 부상 | 2건 (경기당 0.08명 — 커널 주석의 "0.45명" 은 과대평가입니다) |
| 퇴장 | 5건 — **교체하지 않고 열 명으로 뜁니다.** 축구 규칙대로입니다 |
| 교체 선수의 2D 표시 | 정상 — 교체가 일곱 번 일어난 경기도 내내 `11v11` 이었습니다 |

부상 두 건 모두 **72분 부상 → 73분 교체**, **76분 부상 → 77분 교체**였습니다.
원인은 `subCheck()` 가 분이 바뀔 때 한 번만 돌기 때문(`this._subMin`)이고,
`processHurt` 에서 그 자리에서 채우도록 `SUB-03` 패치를 넣었습니다.

> ⚠ **`-audit N` 의 결과는 `-n 1` 과 다를 수 있습니다.** 한 VM 에서 여러 판을 잇달아
> 돌리므로 선수 객체(`p.inj`·`p.ban`)가 판을 넘어 남습니다. 한 판을 정확히 재현하려면
> `-home <홈> -away <원정> -record` 로 단독 실행하세요.

`SUB-03` 은 **부상이 났을 때만** 도는 경로라, 부상이 없는 경기의 지문은 한 글자도
바뀌지 않습니다(울산 vs 전북 = `aaa6474d` 그대로).

### 브라우저 없이 확인하기

```bash
cd tools/realmatch && go run . -root ../.. -home ulsan -away jeonbuk -n 2
```

실제 명단으로 90분을 돌립니다. 선수 항목이 하나라도 빠지면 여기서 걸리고,
같은 라인업이면 지문이 같아야 하므로 **실제 데이터에서의 결정론**도 함께 봅니다.
기준값은 **울산 vs 전북(자동 라인업) = 지문 `aaa6474d`** 입니다 (2026-08-14 기준).

> ⚠️ **여기 지문과 경기 화면 지문은 다릅니다.** 시드를 뽑는 곳이 다르기 때문입니다 —
> 이 도구는 `planSig`(라인업 객체), 경기 화면(`matchworker`)은 **대전 코드 두 개**입니다.
> 일부러 그렇게 뒀습니다(도구는 코덱·데이터 해시를 들고 오지 않습니다 —
> `tools/realmatch/main.go` 주석). **두 값을 견주지 마세요.**
> 선수 데이터나 커널 패치를 바꾸면 지문도 당연히 달라지므로 그때는 새 값으로 다시 잡습니다.
> (옛 기준 `deaca9c0` 은 2026-08-13 판의 값이라 지금은 안 나옵니다.)

같은 구단끼리도 여기서 볼 수 있습니다. 한쪽 포메이션을 갈라 주면 됩니다.

```bash
cd tools/realmatch && go run . -root ../.. -home ulsan -away ulsan -awayform 3-5-2 -record
```

`-record` 를 붙이면 **선수 id 가 제 쪽에 있는지**까지 셉니다
(`명단 40명 (홈 20 · 원정 20) · id 갈림 정상`). 하나라도 섞이면 그 자리에서 중단합니다.

> ⚠️ 이 도구는 모듈을 이어 붙여 돌리므로 `export` 목록이 빠져도 통과합니다.
> 실제로 `kernel.js` 가 `F_` 를 내보내지 않아 **브라우저에서는 경기가 아예 시작되지 않는데도**
> 이 도구는 멀쩡히 돌던 적이 있습니다. 화면 확인을 완전히 대신하지는 못합니다.

---

## 단계 8 — 조건부 지시 ✅

듀얼에는 **경기 중 개입이 없습니다.** 코드를 주고받은 뒤에는 아무도 버튼을 누르지 못하므로,
"이렇게 되면 이렇게 하라"를 미리 적어 두는 것이 전략의 핵심입니다.

라인업 화면 **📋 조건부 지시** 카드에서 여섯 칸까지 적습니다 — 칸마다 조건 하나와 행동 하나.

| | 고를 수 있는 것 |
|---|---|
| 조건 15종 | 15·30·60·75분 · 전반 종료 · 지고/이기고/비기고 있으면 · 60분에 지고/이기고 · 75분에 지고/이기고 · 두 골 이상 앞서/뒤지면 · 퇴장이 나오면 |
| 행동 15종 | 총공세 · 공격적으로 · 잠근다 · 실리로 · 압박 ↑↓ · 템포 ↑↓ · 길게/짧게 · 측면 벌리기/좁히기 · 거칠게 · 카드 아끼기 · 역습 켜기 |
| 프리셋 3종 | 기본기 · 끝까지 밀어붙인다 · 지키는 축구 — 설계서가 "초보자용 프리셋을 꼭 넣으세요"라고 못박은 자리입니다 |

- **한 칸은 한 번만** 발동합니다. 분마다 다시 걸리면 같은 지시가 90번 나갑니다.
- 발동하면 해설에 `📋 울산, 미리 적어 둔 지시 — 총공세로 나섭니다. (60분에 지고 있으면)` 로 남습니다.
  이벤트 종류는 **`order`** 입니다 — 화면이 이걸 보고 해설이 아닌 **금색 카드**로 세우고,
  경기가 끝나면 발동 목록을 요약해 보여 줍니다(`drawOrderLog`).
  ⚠ **문구는 건드리지 마세요.** `fingerprint()` 가 이벤트 본문을 먹으므로 한 글자만 바꿔도
  이미 나눠 가진 결과 링크의 지문이 달라집니다.
- 판정은 **분 경계에서만**, `syncClock` 에 붙여 봅니다(연습 모드의 `aiTacticCheck` 와 같은 자리).
- ⚠ **난수를 뽑지 않습니다.** 판정에 난수가 끼면 같은 코드가 다른 경기를 냅니다(D-1).
  지시가 없는 팀에게는 경기가 한 글자도 달라지지 않습니다 (지시 없는 대진의 지문이 불변으로 확인).
- 지시는 **대전 코드에 함께 담깁니다** — 단계 5가 48비트를 미리 비워 뒀으므로 코드 길이는 51자 그대로입니다.
  지시가 다르면 시드도 달라집니다(다른 경기여야 하므로). 지시가 하나도 없으면 예전 지문이 그대로 나옵니다.

### 얼마나 늘릴 수 있나

코드 예산이 곧 한도입니다. 지금 배분은 **300비트**를 쓰고, 버퍼는 38바이트(304비트)라
**4비트가 비어 있습니다.** Base64url 한 자가 6비트이므로 "코드를 몇 자 늘릴지"가 예산입니다.

| 늘리는 것 | 칸당 비트 | 총 비트 | 코드 길이 |
|---|---|---|---|
| **지금** — 조건 15종 · 행동 15종 · 6칸 | 8 | 300 | **51자** |
| 조건·행동을 각 31종으로 | 10 | 312 | 52자 |
| 칸을 8개로 (종류는 그대로) | 8 | 316 | 53자 |
| 칸 8개 + 각 31종 | 10 | 332 | 56자 |
| 칸 12개 + 각 31종 | 10 | 372 | 62자 |
| 칸 6개 + 조건 63종 + 행동 63종 | 12 | 324 | 55자 |

- 4비트 = **15종**입니다(0은 빈 칸). 5비트면 31종, 6비트면 63종입니다.
- 결과 링크는 코드 두 개라 늘어난 자릿수의 두 배가 붙습니다. 지금 152자이고,
  62자 코드로 가도 174자로 게시판 링크 인식 범위 안입니다.
- ⚠ 배분을 바꾸면 **엔진 버전을 올려야** 하고, 옛 코드는 "상대와 버전이 다릅니다"로 거부됩니다.
  그래서 늘릴 것은 **한 번에 모아서** 올리는 게 낫습니다.

**비트보다 먼저 걸리는 것** — 지금 조건이 볼 수 있는 정보는 `분 · 득실차 · 내 퇴장 수` 셋뿐입니다.
엔진에는 슈팅·유효슈팅·파울·경고·점유율·상대 퇴장·체력이 다 있으므로, 조건 종류를 늘리는 일은
비트가 아니라 **무엇을 보게 할지 고르는 문제**입니다. 행동도 지금은 슬라이더만 건드립니다 —
선수 교체·역할 변경까지 넣으려면 "누구를"이 필요해 칸마다 6비트가 더 붙습니다(칸당 14비트).

**검토 기준 통과** — "60분에 지고 있으면 총공세"를 넣고 약팀(김해)으로 전북과 붙였더니
`60' 김해 📋 김해, 미리 적어 둔 지시 — 총공세로 나섭니다. (60분에 지고 있으면)` 이 떴습니다.
이기고 있던 경기에서는 발동하지 않았습니다.

---

## 단계 5 — 대전 코드 ✅

라인업 한 벌을 **300비트 = 38바이트 = Base64url 51자**로 담습니다. 설계서 4-1 의 배분 그대로입니다.

```
KM26D1-BhHsDhIjFNJKMvuvABpqsRClrAAAERBMQgqgJRpJQAAAAAAAMQA
```

| 항목 | 비트 | | 항목 | 비트 |
|---|---|---|---|---|
| 엔진 버전 | 6 | | 역할 (11×4) | 44 |
| 데이터 해시 | 16 | | 임무 (11×2) | 22 |
| 구단 | 5 | | 슬라이더 (8×3) | 24 |
| 포메이션 | 4 | | 역습 | 1 |
| 선발 (11×6) | 66 | | 조건부 지시 (6×8) | 48 |
| 교체 (9×6) | 54 | | 체크섬 | 10 |

- **선수 id 대신 명단 번호**를 담습니다. id 는 1~1024 라 10비트가 필요하지만, 한 구단
  명단은 최대 51명이므로 6비트로 줄어듭니다. 구단이 코드 안에 있으니 되돌릴 수 있습니다.
- 선발은 **자리 순서대로** 나열하므로 자리 자체를 담지 않습니다(설계서에서 44비트를 없앤 자리).
- 역할·임무는 **그 자리에서 고를 수 있는 목록 안의 번호**입니다. 자리마다 목록이 다르므로
  4비트(최대 16종)·2비트(한 역할의 임무 최대 3종)로 들어갑니다.
- 조건부 지시 48비트는 단계 8 을 위해 **미리 자리를 잡아 뒀습니다** — 지금은 0 이 들어가고,
  단계 8 이 와도 코드 규격이 바뀌지 않습니다.

| 링크 | 쓰임 |
|---|---|
| `match.html#c=<코드>` | **초대** — 내 코드를 건넨다. 받은 사람 화면의 원정 칸에 그 팀이 앉는다 |
| `match.html#m=<코드A>.<코드B>` | **결과** — 코드 두 개면 누구나 같은 경기를 재현한다. 제3자 중재도 된다 |

시드는 양쪽 코드에서 함께 유도되므로(D-1) 어느 쪽도 유리한 시드를 골라 보낼 수 없습니다.
결과 화면에 **결과 지문**을 함께 적어 두므로, 양쪽 값이 다르면 그 자리에서 드러납니다.

**조용히 다른 라인업으로 해석되는 것이 최악**이므로(설계서 4-2) 어긋나면 반드시 거부합니다.

| 어긋난 것 | 화면에 뜨는 말 |
|---|---|
| 한 글자 손상 | 코드가 잘렸거나 손상되었습니다 (체크섬 불일치) |
| 잘린 코드 | 51자여야 하는데 48자입니다 |
| 규격 판 다름 | 상대와 버전이 다릅니다 — 코드는 v2, 이 화면은 v1 입니다 |
| 명단 다름 | 선수 명단이 서로 다릅니다 — 같은 판의 데이터로 맞춰야 합니다 |

### 확인 — Go 로 규격을 지킨다

```bash
cd tools/codecheck && go run . -root ../..
```

**코드 규격을 Go 로 다시 옮겨 적지 않습니다.** 두 벌을 유지하면 반드시 어긋나고, 어긋난
코드는 조용히 다른 라인업이 됩니다. 대신 goja 로 `src/codec/duelcode.js` 를 그대로 돌려
구단 4곳 × 포메이션 12종 × 역할 배치 3가지 = **144건**을 검사합니다.

- 왕복 — 라인업 → 코드 → 라인업 → 코드 가 한 글자도 다르지 않은가
- 내용 — 선발·교체·역할·임무·슬라이더·조건부까지 되돌아왔는가
- 손상 — 한 글자를 바꾸면 **반드시** 거부되는가
- 벡터 — 정해진 라인업이 정해진 코드가 되는가 (`-vector <코드>` 로 못박을 수 있습니다)

---

### 연습 모드 (단계 A) ✅

상대가 없어도 혼자 전술을 시험합니다. 경기 화면 **원정 칸에서 `🤖 연습 · 쉬움/보통/어려움`**
을 고르면 그 구단을 AI 감독이 맡습니다. 팀마다 **3난이도 × 전술 3종 = 아홉 가지** 상대입니다.

**난이도는 선수 능력치를 건드리지 않습니다.** 전력이 떨어져도 전술로 이기는 게 재미라는
D-4 판단이 여기서도 유효하므로, 조절하는 것은 상대 감독이 얼마나 유능한가입니다.

| 난이도 | 라인업 | 후반 대응 | 전술 |
|---|---|---|---|
| 쉬움 | 명단 순서대로 (자리에 안 맞는 선수가 섞인다) | 없음 | 고른 대로 |
| 보통 | 자리마다 능숙도 × 별점 최고 | `aiTacticCheck` | 고른 대로 |
| 어려움 | 커널 `slotRating` 으로 조합 전체 최적화 | 〃 | **내 슬라이더를 보고 상성으로** |

- 상성: 내가 라인을 올리면 **역습**, 좁게 서면 **측면 공략**, 짧게 돌리면 **높은 압박**.
- `aiTacticCheck` 는 원본에 있으나 호출부가 UI 계층이라 죽어 있었습니다. `syncClock`
  (분 경계)에 붙여 되살렸고, **`team.autoTactic` 이 켜진 팀만** 움직입니다 —
  대전 양 팀은 지시한 그대로 갑니다(지시 없는 대진의 지문이 불변으로 확인).
- **연습은 매 판 새 시드**입니다. 같은 판만 반복되면 연습이 되지 않으니까요.
  대신 결과 코드를 발급하지 않습니다 — 공유할 대상이 아닙니다.
- ⚠ `slotRating` 은 리그 평균(`attrMeans` → 전역 `G`)을 보므로, AI 라인업을 짜기 **전에**
  일꾼이 문맥을 한 번 세웁니다. 경기 시작 때 같은 두 팀으로 다시 세우므로 평균은 그대로입니다.

난이도는 **능력치를 올려서 만들지 않습니다.** 그러면 "전력이 떨어져도 전술로 이기는 재미"가 무너집니다.
대신 상대 감독이 얼마나 유능한지를 바꿉니다 — 라인업을 잘 짜는가, 경기 중에 대응하는가,
내 전술을 보고 상성으로 골라 오는가.

연습 모드는 코드·링크 규격이 필요 없어서 **대전보다 먼저 나옵니다.** 자세한 설계는 설계서 단계 A.

---

## 전술 게시판 (`board.html` · `src/board/board.js`)

상대를 구하는 일이 번거로워 아무도 안 한다는 제보에서 출발했습니다.
백엔드는 **Supabase** 하나이고, 켜는 절차는 [docs/전술게시판-설정.md](docs/전술게시판-설정.md) 에 있습니다.

### 붙는 자리를 두 군데 둡니다

| 어디 | 무엇을 위해 |
|---|---|
| 경기 화면 원정 칸 | **지금 붙을** 상대를 그 자리에서 고른다. 페이지를 옮기지 않는 것이 핵심이라 여기가 본진입니다 |
| `board.html` | **둘러보는** 자리. 카드로 펼치고, 찾고, 구단 색·포메이션까지 봅니다 |

경기 화면의 목록은 **라인업 목록보다 위**에 둡니다. 처음에는 `🔗 대전 코드 붙여넣기`
상자 안에 들어 있었는데, 그러면 **코드 칸을 이미 고른 사람에게만** 보입니다 —
정작 이 기능이 필요한 사람은 붙을 코드가 없는 사람입니다.

목록에서 한 줄을 고르면 `#awayPlan` 을 `code` 로 **대신 골라 줍니다.** 안 그러면 코드는
들어갔는데 칸은 `자동 라인업` 그대로라, 고른 라인업이 아닌 다른 팀과 경기가 시작됩니다.

### 듀얼 기록실 (`record.html` · `matches` 표)

대전은 끝나면 그걸로 사라졌습니다. 결과 링크를 주고받은 둘만 알고, 남들은 **누가 누구와
붙어서 어떻게 됐는지**를 볼 길이 없었습니다. 게시판에 라인업이 올라와도 "이걸 실제로
세 보니 어떻더라"가 안 쌓입니다.

그래서 경기가 끝나면 **결과 링크를 그대로** 올립니다. 링크 하나면 누구나 그 경기를 토씨
하나 안 틀리고 다시 볼 수 있으므로(시드가 코드 두 장에서 나옵니다), 기록실은 곧
**다시 볼 수 있는 경기 목록**입니다.

| 무엇 | 어떻게 |
|---|---|
| 올리는 조건 | ① 게시판이 켜져 있고 ② 연습이 아니고 ③ **양쪽 다 사람이 짠 전술**(`auto` 가 한쪽이라도 끼면 안 올린다) |
| 중복 방지 | 표의 `unique (h_code, a_code)`. **같은 코드 두 장은 언제나 같은 경기**라, 이게 곧 "같은 경기"의 정의다 |
| 중복일 때 | 오류가 아니라 `"dup"` 을 돌려주고 "이미 올라와 있습니다"로 알린다 |
| 닉네임 | 게시판(`plans`)에서 **코드로 찾아온다.** 내가 직접 짠 쪽만 이 브라우저에 적어 둔 이름(`myNick`)을 쓴다 |
| 실패해도 | 경기 결과와 결과 링크는 그대로다 — 기록실은 **덤**이다 |

> ⚠ **닉네임은 신원 확인이 아닙니다.** 브라우저에 적어 둔 이름일 뿐입니다.
> 그래도 문제되지 않는 이유는 기록실이 **대전 코드**를 함께 들고 있어서,
> 이름이 틀려도 경기 자체는 누구나 재현해 검증할 수 있기 때문입니다.

> ⚠ **`url` 칸을 그대로 링크로 쓰지 마세요.** 올린 사람의 브라우저 기준 주소라
> `http://localhost:8123/...` 같은 것이 섞입니다. 기록실 화면은 **저장된 코드 두 장으로
> 지금 여기서** 링크를 다시 만듭니다(`resultLink`). `url` 은 올린 그대로 남겨 둘 뿐입니다.

`matches` 표를 안 만들어도 게임은 그대로 돕니다 — 기록실만 "아직 준비 중"으로 뜹니다.
PostgREST 가 없는 표에 `does not exist` 를 주므로, 그 문구를 보고 **설정이 덜 된 것**과
**진짜 오류**를 갈라 알립니다.

### 결과 링크는 열자마자 시작합니다

`#m=코드A.코드B` 는 양쪽 라인업이 이미 다 정해진 주소입니다. 고를 것이 하나도 없는데
대진 화면을 보여 주고 `경기 시작`을 한 번 더 누르게 하는 것은 군더더기였습니다.
`boot()` 끝에서 바로 `start()` 를 부릅니다.

> ⚠ 초대 링크(`#c=코드`)는 **그대로 둡니다.** 그건 상대 팀만 정해진 것이라
> 내 라인업을 골라야 경기가 성립합니다.

### 라인업 성적 — 저장하지 않고 그 자리에서 낸다

기록실에는 **어느 코드가 어느 코드와 붙어 몇 대 몇이었는지**가 다 남아 있습니다.
게시판의 라인업도 코드로 구분되므로, 둘을 **코드로 맞추면** 그 전술이 실제로 얼마나
통했는지가 나옵니다. 그래서 집계 칸을 따로 두지 않았습니다 —
경기 목록을 받아 브라우저에서 셉니다(`recordsByCode`).

이렇게 하면 기록이 **언제나 경기와 일치**합니다. 집계를 따로 저장하면 경기를 감추거나
지웠을 때 숫자가 어긋나고, 그걸 맞추는 코드가 또 필요해집니다.

- **홈·원정을 갈라서** 셉니다. 같은 전술이라도 홈에서 밀어붙이는 것과 원정에서
  받아치는 것은 다른 이야기라, 합쳐 놓으면 그 차이가 사라집니다.
- 성적을 못 읽어도(기록실 표가 없는 배포 등) 게시판은 그대로 뜹니다 — **덤**입니다.

> ⚠ **승률로 줄 세우면 안 됩니다.** 한 판 이긴 라인업(100%)이 9승 1패(90%)보다 위로 옵니다.
> `planScore` 는 **경기당 승점**을 쓰되 표본이 적으면 평균(1.5점) 쪽으로 당깁니다
> — 가상의 무승부 한 판을 얹는 셈입니다(`PRIOR_N=1, PRIOR_PTS=1.5`).
> 덤으로 **0경기짜리 새 글이 딱 평균 자리**에 서므로, 맨 아래로 가라앉지도 않고
> 이유 없이 맨 위에 오지도 않습니다.
>
> | 라인업 | 승률 | planScore | 줄 |
> |---|---|---|---|
> | 10승 1무 2패 | 77% | (31+1.5)/14 = **2.32** | 1 |
> | 1승 (한 판) | 100% | (3+1.5)/2 = **2.25** | 아래 |
> | 아직 안 붙어 봄 | — | 1.5/1 = **1.50** | 가운데 |
> | 0승 2패 | 0% | 1.5/3 = **0.50** | 맨 아래 |

### 찾기는 서버에 다시 묻지 않습니다

글자를 칠 때마다 요청을 보내면 무료 티어를 그것만으로 씁니다. 한 번 받아 둔 목록
(경기 화면 120줄 · 게시판 200줄)을 **브라우저에서 거릅니다.** 그래서 한 글자만 쳐도
기다림이 없습니다.

- 닉네임 · 구단 · 한 줄 소개를 **칸마다 따로** 봅니다. 이어 붙여 찾으면 닉네임 "철수" +
  구단 "안양" 이 "철수안양" 이 되어 있지도 않은 `수안` 이 걸립니다.
- **초성**(`ㅇㅇ` → 안양)은 **초성만 친 경우에만** 씁니다. 안 그러면 `가` 가 `각` 에도 걸려
  검색이 엉뚱하게 넓어집니다.

`board.html` 은 목록의 코드를 그 자리에서 `decodePlan` 으로 풀어 **포메이션과 구단 색**을
붙입니다. 실패해도 줄은 그대로 보여 줍니다(옛 판 코드·손상 코드에 꼬리표만 답니다) —
데이터를 아예 못 읽어도 목록·찾기·붙기는 그대로 됩니다. 보여 주기 위한 **덤**입니다.

---

## KM26 갈래 (`km-lineup.html` · `src/km26/kmsave.js`)

### 2026 갈래는 화면에서만 감췄습니다 (2026-08-17)

갈래가 둘이면 첫 화면부터 갈림길이 생기고, 왼쪽 바에 화면이 열 개 늘어섭니다.
이 게임의 값어치는 **내가 키운 팀으로 붙는 것**에 있으므로 KM26 갈래 하나만 남겼습니다.

**지운 것은 없습니다.** 손댄 곳은 두 군데뿐입니다.

- 열 개 화면 전부에서 `navbtn` 다섯 줄(2026 라인업·게시판·기록실·경기·선수단) 제거
- `index.html` 본문을 KM26 한 줄기로 다시 씀 (갈래 설명 칸 삭제)

`lineup.html`·`board.html`·`record.html`·`match.html`·`squad.html` 과 그 아래 코드
(`src/codec/duelcode.js` 의 51자 코드 포함)는 **그대로 살아 있습니다.** 주소를 직접 치면
열리고, 이미 나눠 가진 대전 코드·결과 링크도 계속 동작합니다.

> ⚠ **되돌리려면** 그 다섯 줄을 다시 넣고 `index.html` 본문을 복원하면 됩니다.
> 엔진·코덱·데이터는 손대지 않았으므로 재생성이나 도장은 필요 없습니다.
> ⚠ 2026 화면들도 왼쪽 바가 KM26 만 보이도록 함께 고쳤습니다 — 주소로 들어와도
> 돌아갈 길이 KM26 쪽으로 이어집니다.

### 저장 슬롯은 맨 위 · 담는 것은 명단 **전체**

처음에는 슬롯을 화면 맨 아래 "저장하고 붙기" 카드에 두고, `pack` 이 있으면 저장,
없으면 아무것도 안 하게 두었습니다. **불러오는 길이 아예 없었습니다.** 그래서 이 화면에
들어올 때마다 1MB짜리 세이브 파일을 처음부터 다시 골라야 했습니다(사용자 제보).

2026 라인업 화면은 이미 답을 갖고 있었습니다 — **슬롯이 맨 위**에 있고, 카드를 누르면
바로 올라오고, 없애는 것만 한 번 묻습니다. 같은 자리·같은 규칙으로 맞췄습니다.
두 화면을 오가는 사람이 매번 다시 배우지 않아도 되는 쪽이 낫습니다.

**슬롯에는 줄이지 않은 팩을 담습니다.** 게시판에 올릴 때는 `slimPack` 으로 경기에 나올
스무 명만 담지만, 슬롯은 다음에 와서 **라인업을 다시 짜는** 자리입니다. 스무 명만 담으면
불러온 뒤 `그 외 명단`이 텅 비어 선수를 바꿔 넣을 수가 없습니다.
실측으로 38명 한 벌이 **46KB** 라 다섯 칸을 다 채워도 브라우저 저장소에 부담이 없습니다.

> ⚠ 옛 슬롯(줄인 팩)도 그대로 읽습니다 — `fatten` 이 되살립니다. 그때 저장한 것에는
> 스무 명만 들어 있어 `그 외 명단`이 0명으로 보이는데, **고장이 아닙니다.**

슬롯에서 온 상태에는 세이브 파일이 없습니다(`G === null`). 그래서 `↩ 세이브가 정한 대로`를
끕니다 — 되돌릴 원본이 없습니다. `↻ 자동 배치`는 지금 명단만 있으면 되므로 그대로 둡니다.

### 게시판은 두 자리에 둔다 — 없앴다가 되돌린 기록

KM26 갈래는 코드 발급이 없어 "라인업 짜는 자리에서 올리고 고르는" 흐름이 맞다고 보고,
게시판 화면을 없애고 `km-lineup` 안에 목록만 넣었습니다. 흐름은 실제로 나아졌는데
**전적을 볼 자리가 같이 사라졌습니다** — 라인업 안의 목록은 "누가 올렸나"만 보여 주고
승률·승무패를 얹을 자리가 없습니다(그 카드는 이미 열 개짜리 화면의 맨 아래입니다).

되돌리되 **역할을 갈랐습니다.**

| 자리 | 무엇을 하나 |
|---|---|
| `km-lineup` 안의 목록 | 짜던 자리에서 **바로 상대 고르기.** 닉네임·구단·한 줄 소개만 |
| `km-board.html` | **전적을 보는 곳.** 전술별 승률·승무패·승점(홈/원정 분리) · 구단별 표 |

> ⚠ 목록을 고칠 때는 **두 곳 다** 보세요. 찾기(초성 포함)는 두 곳에 각각 있습니다.

**구단별 전적은 구단의 세기가 아닙니다.** 같은 안양이라도 사람마다 명단이 다릅니다
(각자의 세이브입니다). 그래서 "그 구단으로 나온 사람들을 다 합친 성적"이라고 화면에
적어 두었습니다. 그래도 쓸모가 있습니다 — 이 갈래에서 어느 구단이 자주 나오고
얼마나 통하는지가 보입니다.

집계 칸을 따로 두지 않고 **경기 목록에서 그 자리에서 셉니다**(`kmRecordsByTeam`).
기본 갈래와 같은 판단입니다 — 따로 두면 경기를 감췄을 때 숫자가 어긋나고,
그걸 맞추는 코드가 또 필요합니다.

### 명단 지문은 **경기에 나올 스무 명**만 센다

같은 라인업인데 **어느 길로 왔느냐에 따라 다른 경기**가 나오고 있었습니다.

지문(`squadSig`)을 `pack.squad` **전체**에서 뽑고 있었는데, 팩이 담기는 모양이 자리마다 다릅니다 —
게시판은 `slimPack` 으로 **스무 명**, 저장 슬롯은 **명단 전체**(실측 38명). 지문이 갈리면
씨앗이 갈리고, 씨앗이 갈리면 다른 경기입니다.

이제 `plan.xi` + `plan.bench` 에 든 사람만 셉니다. 줄인 팩과 안 줄인 팩이 **같은 값**을 냅니다.
경기 화면도 들어오는 팩을 `slimPack(fatten(p))` 로 한 번 걸러 **늘 스무 명**으로 맞춥니다.

이게 성립해야 **결과 링크**가 성립합니다 — 링크를 연 사람은 게시판에서 스무 명짜리 팩을
받아 오는데, 원래 돌린 사람은 슬롯의 38명짜리로 돌렸을 수 있기 때문입니다.

> ⚠ 이미 게시판에 올라간 글의 `sig` 는 옛 정의로 저장돼 있어 안 맞습니다.
> KM26 갈래를 **공개하기 전에만** 고칠 수 있는 종류의 변경입니다.

### 결과 링크는 코드가 아니라 **글 두 개**를 가리킨다

2026 갈래는 51자 코드 두 개를 링크에 담습니다(`#m=<코드>.<코드>`). KM26 갈래는 선수와
능력치가 사람마다 달라 코드에 담기지 않으므로, 대신 **게시판 글 번호 둘**을 담습니다 —
`km-match.html#m=<홈 글>,<원정 글>`.

여는 쪽은 두 글의 팩을 받아 와 그 자리에서 90분을 다시 돌립니다. 결과를 저장해 두는 것이
아니라 **다시 계산**하는 것이라, 링크가 가리키는 글만 살아 있으면 언제든 같은 경기가 나옵니다
(그래서 위의 지문 규칙이 필요합니다).

그래서 **양쪽 다 게시판에 올라와 있어야** 링크가 나옵니다. 안 올라온 경우에는
그 자리에서 올릴 수 있게 버튼을 줍니다 — 올리는 것은 남에게 보이는 일이라 스스로 누를 때만 합니다.

### 장면 재생에 세대 번호를 붙인다

장면(clip)은 `setTimeout` 으로 프레임을 한 장씩 넘깁니다. 그런데 재생 중에 **판을
갈아엎을 수 있습니다** — `↻ 다시 대진 고르기`·상대 바꾸기·다음 경기 시작.
그러면 `resetForNext()` 가 `R = null` 로 두는데, **아직 예약돼 있던 다음 프레임이
한 번 더 깨어나** `drawFrame` 의 `R.roster` 에서 넘어집니다.

타이머 콜백에서 던진 오류는 아무도 못 받습니다. 그래서 `inClip` 이 `true` 로 남고,
`tickMinute` 이 그 값을 보고 매번 그냥 돌아가 **다음 경기의 시계가 영영 멈춥니다.**

`playClip` 이 시작할 때 세대 번호를 하나 올려 잡아 두고(`const my = ++clipRun`),
매 프레임에서 `my !== clipRun` 이면 조용히 끝냅니다. `resetForNext`·`startReplay` 는
번호만 올리면 됩니다 — 예약된 타이머를 일일이 찾아 지울 필요가 없습니다.

> ⚠ 이 화면은 **확인용 창에서 시간 흐름을 볼 수 없습니다.** 창이 늘 숨어 있어
> `requestAnimationFrame` 은 아예 안 돌고 `setTimeout` 은 1초에 한 번으로 묶입니다
> (프레임 하나에 1초 = 장면 한 편에 2분). 재생·자막 흐름·득점자 패널이 뜨는 **시각**은
> 사람이 실제 화면에서 봐야 합니다. 여기서 확인할 수 있는 것은 오류 유무와 최종 상태뿐입니다.

### 지문은 접어서 넣는다 — 자르면 안 된다

게시판 표가 `sig` 를 **8~64자**로 묶어 두었는데(`check (char_length(sig) between 8 and 64)`)
`sigOf` 가 내는 값은 **244자**입니다. 올릴 때 `slice(0, 64)` 로 말없이 잘라 넣고 있었습니다.
두 가지가 한꺼번에 망가졌습니다.

| 잘라서 생긴 일 | 화면에 어떻게 보였나 |
|---|---|
| 전체 지문으로 `findPlanId` 를 부르면 안 걸린다 | 올렸는데 "내 라인업이 아직 게시판에 없습니다" |
| 앞 64자가 같으면 `unique (sig)` 에 걸린다 | 다른 라인업인데 "이미 올라와 있습니다" |

앞 64자는 **구단 이름과 선발 여덟 자리**쯤에서 끝납니다. 실측으로 교체 두 자리를
맞바꾼 것과 압박을 한 칸 올린 것이 **셋 다 같은 값**이었습니다.

`sigKey()` 로 전체를 **16자리로 접어서** 넣습니다. 길이가 늘 같아 잘릴 일이 없고,
16진수뿐이라 주소 filter 에도 안전합니다. 표는 안 고쳐도 됩니다.

> ⚠ **씨앗에는 접기 전 지문을 씁니다.** 접은 값은 "같은 글인가"를 가릴 때만 씁니다 —
> 이미 기록실에 남은 경기의 결과를 흔들지 않으려는 것입니다.
> ⚠ `kmPostPlan` 은 이제 길이가 안 맞으면 **거부합니다.** 조용히 자르는 코드를 다시
> 넣지 마세요 — 잘린 지문은 아무 오류도 안 내고 몇 판 뒤에야 증상이 나타납니다.

### 글 번호는 지문으로 다시 찾는다

기록실에 남기려면 내 글 번호가 필요한데, 예전에는 **라인업 화면에서 그 순간 올린 경우에만**
그 값을 들고 있었습니다. 저장 슬롯에서 왔거나 지난번에 이미 올려 둔 경우에는 비어 있어서,
게시판에 멀쩡히 올라와 있는데도 "먼저 게시판에 올려야 합니다"가 떴습니다(사용자 제보).

`findPlanId(sig)` 로 **먼저 찾아봅니다.** 지문이 곧 그 라인업이므로 이 조회는 언제나 맞습니다.

### 화면 글에 `\uXXXX` 가 남는 사고

게시판 상대 찾기 칸과 경기 화면의 재생 단추가 `게시...` 처럼 **부호 그대로**
보이고 있었습니다. **JS 문자열 안에서는 멀쩡한 표기가 HTML 본문·속성에서는 아무 뜻이 없어서**
그대로 글자로 나옵니다.

편집 도구가 **이미 escape 된 자리를 고치면 새로 쓰는 글자도 같은 표기로 이어 씁니다.**
그 자리에 한글을 그냥 써 넣으면 다시 `\uXXXX` 가 됩니다. 되돌릴 때는 스크립트로
**그 줄만** 짚어 풀고, 몇 줄이 걸렸는지 찍은 뒤 화면에서 눈으로 확인했습니다.

확인은 `<script>` 밖에 `\uXXXX` 가 남았는지 전 화면을 훑는 것으로 합니다.
`jscheck` 는 이걸 못 잡습니다 — 문법으로는 아무 문제가 없기 때문입니다.

---

## 출처와 권리

이 프로젝트의 매치엔진은 **[KM26 v2.0](https://github.com/KleagueM2026/KM26v2.0)** 에서 가져왔습니다.
원저작자의 사용 허락을 받았습니다. 자세한 내용은 [NOTICE.md](NOTICE.md)를 보세요.

**비공식 팬 프로젝트입니다.** 한국프로축구연맹 및 각 구단과 아무 관련이 없으며, 구단 엠블럼을 사용하지 않습니다.
